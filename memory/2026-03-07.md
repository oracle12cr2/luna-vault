# 2026-03-07 (토)

## OEM 13c R5 설치 — 계속 진행 중

### 완료
- SYSMAN user + 관련 tablespace drop 완료 (RAC 3노드)
- RepManager emrepmgr.pl 수정: `GRANT DBA TO $EM_REPOS_USER` 추가 (createReposUser에서 grants 호출 안 하는 버그 패치)
  - 주의: `print SQL_FILE "exit;\n"` sed 치환 시 전체 파일에 적용됨 → 라인 번호(1337) 지정해야 함
- RepManager 시도 #5: SYS credentials check에 grants 코드가 들어가서 실패 (sed 전체 치환 문제)
- RepManager 시도 #6: user 생성 OK, schema 생성 실패 — `core_cre.sql`, `db_cre.sql`, `ias_cre.sql`, `ocs_cre.sql` 파일 없음
  - OEM 13c R5의 SQL 구조가 바뀜: 구식 monolithic *_cre.sql 대신 서브디렉토리별 개별 스크립트 사용
- **RCU 발견 및 사용** (`/cloud13cr5/oem/oracle_common/bin/rcu`)
  - RCU로 인프라 스키마 생성 성공: STB, OPSS, IAU, IAU_APPEND, IAU_VIEWER, MDS, WLS (prefix: SYSMAN)
  - 하지만 ConfigureGC.sh prereq에서 `cs_MDSUserExists` 에러 — MDS 유저가 이미 존재하면 fresh install 불가
  - RCU 스키마 전부 drop → DB 완전 클린 상태로 복원
- ConfigureGC.sh 재실행 (14:09) — prereq 통과, omsca 시작됨
  - 결과: `ORA-01017: invalid username/password; logon denied` — SYSMAN 유저가 없어서 omsca가 접속 불가
  - **핵심 모순**: prereq은 SYSMAN 없는 걸 "OK" 하지만, omsca는 SYSMAN 존재를 기대함

### RAC 데이터파일 에러 (해결됨)
- RepManager가 mgmt.dbf를 로컬 파일시스템(`/oracle/.../dbs/`)에 생성 → PROD2에서 ORA-01157 발생
- DROP TABLESPACE로 정리 완료, 3노드 모두 정상 (12 datafiles, 0 recover)
- **교훈**: RAC에서 tablespace 생성 시 반드시 ASM(`+DATA`)에 생성해야 함

### 현재 상태 & 막힌 점
- ConfigureGC.sh/omsca는 SYSMAN 리포지토리가 이미 존재해야 작동
- RepManager는 OEM 13c R5 SQL 구조와 호환 안 됨 (core_cre.sql 없음)
- 3/6 이전 성공 사례에서는 RepManager가 SYSMAN 스키마(21757 objects)를 생성했었음 → 어떻게 가능했는지 조사 필요
- SYSMAN_RO user 여전히 drop 불가 (editions_enabled=Y, 빈 껍데기)

### 추가 시도 (16:00~16:50)
- SYSMAN 유저 수동 생성 + MGMT_VERSIONS_E 테이블/뷰 → ConfigureGC.sh: "valid SYSMAN but not OPSS"
- OPSS 체크 이름: `sysman122140_opss` (prefix SYSMAN122140, OEM 내부 WLS 12.2.1.4.0 기반)
- MDS는 `sysman_mds` (prefix SYSMAN)으로 정상
- RCU로 SYSMAN122140_OPSS 생성 성공
- ConfigureGC.sh 재시도: `ORA-00904: SYSMAN.EM_UPGRADE_UTIL.GET_LAST_PHASE` → SYSMAN에 PL/SQL 패키지 없음
- **결론**: ConfigureGC.sh는 재구성 도구일 뿐, 완전한 SYSMAN 스키마가 필요. 풀 인스톨러만 스키마 생성 가능.
- 풀 인스톨러(`em13500_linux64.bin`)는 기존 홈이 비어있지 않으면 거부 ("not empty" 에러)
- OEM 홈 detachHome 해도 파일시스템 체크에서 걸림

### 풀 인스톨러 시도 (16:58~17:18)
- 기존 홈 mv: `/cloud13cr5/oem` → `oem_old`, `/cloud13cr5/agent` → `agent_old`
- DB 스키마 전부 drop 완료 (SYSMAN, SYSMAN_*, SYSMAN122140_* 모두)
- 빈 디렉토리 생성: `/cloud13cr5/oem/`, `/cloud13cr5/agent/`
- zip 파일 추출: `/cloud13cr5/em13500_linux64-{2,3,4,5}.zip` → `/tmp/`
- response file에 `INSTALL_TYPE="Oracle Management Server Complete"` 추가
- **결과**: 인스톨러가 validation만 하고 SW 설치 단계로 안 넘어감!
- **원인**: 3/4 원래 설치는 GUI 모드(`s_silent=false`), `-silent`에서 SW 설치 건너뛰는 버그
- **해결 필요**: X11/VNC로 GUI 설치 또는 다른 방법

### 현재 상태
- `/cloud13cr5/oem/` = 빈 디렉토리
- `/cloud13cr5/oem_old/` = 기존 OEM 홈 (소프트웨어 있음)
- `/cloud13cr5/agent_old/` = 기존 agent 홈
- DB = 완전 클린 (SYSMAN 관련 유저/테이블스페이스 없음)
- 인벤토리 = HOME_LIST 비어있음

### SchemaManager 발견 및 실행 (20:40~)
- `-silent` 인스톨러 문제 해결 못함 (GUI 모드만 동작, xvfb-run+silent도 실패)
- **핵심 발견**: `/cloud13cr5/oem/sysman/admin/emdrep/bin/schemamanager.pl` — 리포지토리 생성 전용 도구
  - 3/4 원래 설치에서도 `"Calling the EMSCHEMA MANAGER: CREATE"` 로 호출됨
  - Java 기반, 내부적으로 RCU 사용 (`"schema manager is created for rcu based action"`)
- oem_old를 `/cloud13cr5/oem`으로 복원
- SchemaManager 실행 시 필요한 환경변수:
  - `COMMON_COMPONENTS_HOME=/cloud13cr5/oem/oracle_common` (필수! `$oracleHome/../oracle_common` 경로가 잘못 해석됨)
  - `MW_HOME=/cloud13cr5/oem`
  - `ORACLE_HOME=/cloud13cr5/oem`
  - `JAVA_HOME=$ORACLE_HOME/oracle_common/jdk`
  - `PERL5LIB=$ORACLE_HOME/sysman/admin/emdrep/bin:$ORACLE_HOME/sysman/install:$ORACLE_HOME/perl/lib`
- 비밀번호 입력: `printf "Oracle2026_em\nOracle2026_em\noracle\noracle\n"` (SYSMAN 2번, SYS 2번)
- **실행 결과**: RCU 정상 시작! SYSMAN, SYSMAN_TYPES, SYSMAN_RO, MGMT_VIEW 유저 생성 완료
- 20:45 시점: EBR 패키지, create_header.sql 등 SQL 스크립트 순차 실행 중
- `EM_REPOS_COMMON` → `EM_REPOS_INIT` 단계 진행 중
- 3/4 원래 설치에서 이 단계 약 17시간 소요 — 장기 실행 예상
- PID: 3149749 (perl), Java 프로세스 2개
- **SchemaManager 로그**: `/cloud13cr5/oem/sysman/log/schemamanager/m_030726_0844_PM/`
- **RCU 로그**: 위 경로 내 `m_030726_0844_PM.CREATE/logs/rcu.log`

### 교훈 정리
- OEM 13c R5 풀 인스톨러 `-silent` 모드: SW 설치 안 됨 (GUI only 버그)
- `schemamanager.pl`이 리포지토리 생성의 올바른 도구 (ConfigureGC.sh/RepManager 아님)
- `COMMON_COMPONENTS_HOME` 환경변수 필수 — 없으면 `RCUFormatter` ClassNotFound
- 남궁건 VNC 환경 준비함 (17:20), 외출 중 (17:23~)

### 다음 단계
1. SchemaManager 완료 대기 (수시간~17시간)
2. 완료 후 ConfigureGC.sh로 OMS 구성
3. 또는 SchemaManager 실패 시 VNC GUI 인스톨러 시도

### 파일 위치
- RepManager 백업: `/cloud13cr5/oem/sysman/admin/emdrep/bin/10.1/emrepmgr.pl.bak`
- admin_create_tablespaces.sql 백업: `.../admin_create_tablespaces.sql.bak`
- 로그: `/tmp/emrepmgr_create5.log`, `/tmp/emrepmgr_create6.log`
- RCU 로그: `/tmp/RCU2026-03-07_10-36_1689083402/logs/`
- omsca 로그: `/cloud13cr5/oem/cfgtoollogs/omsca/omsca_20260307140929.log`
- ConfigureGC 출력: `/tmp/configureGC_output.log`
- **SchemaManager 출력**: `/tmp/schemamanager_output5.log`
- **SchemaManager 로그**: `/cloud13cr5/oem/sysman/log/schemamanager/m_030726_0844_PM/`
