# 2026-02-19 메모

## 컴팩션 전 메모리 플러시
- 8번째 컴팩션 직전 상태 저장

## 진행 중 작업
- 슈퍼 ISA 펀드 정보: 뉴스 URL 찾았으나 본문 fetch + 이메일 발송 미완
- Mac 알림 → 텔레그램 포워딩: 오늘(02/19) 세팅 예정이었음
- Notion API: 사용자 API 키 대기 중

## 대기 중
- 설 연휴 끝남 → 구직 재검색 시점
- 엑사아이엔티: 사용자가 070-8708-2580 전화해서 이메일 확인 필요
- 대치동 PostgreSQL/Tibero 포지션: 설 연휴 후 결정 예정
- 로또 주간 크론: 02/23(월) 자동 발송 확인 필요

## 캘린더 이벤트 (02/19)
- 맥북알림세팅 10:00 AM 예정

## Debezium + Kafka CDC (02/19)
- Docker CE 29.2.1 설치 완료
- docker-compose.yml 생성: `/root/.openclaw/workspace/debezium/docker-compose.yml`
  - zookeeper, kafka, connect (Debezium 2.7), kafka-ui (provectuslabs)
  - 포트: zookeeper 2181, kafka 9092, connect 8083, kafka-ui 8080
- `docker compose up -d` 실행, 이미지 pulling 중 (connect ~303.7MB)
- 다음 단계: 컨테이너 실행 확인 → Oracle supplemental logging → debezium 유저 생성 → connector 등록 → CDC 테스트

## Oracle→PostgreSQL 마이그레이션 (완료 요약)
- ora2pg로 APP_USER 스키마 전체 마이그레이션 완료
- 15 테이블, ~122M 행, Oracle 21.3GB → PostgreSQL 26GB
- 병렬 처리(-j4 -J4) 11배 성능 향상
- DDL + 데이터 + 함수 + 시퀀스 + 인덱스 전부 이관
- PostgreSQL DB: ora2pg_test, 스키마: app_user
- 마이그레이션 리포트 이메일 발송 완료

## Debezium CDC 진행 상세 (22:48~00:18)
- Oracle archivelog 모드 변경 완료, spfile ASM에 재생성
- supplemental logging 활성화 (MIN+ALL = YES)
- debezium 유저 생성 (password: welcome1), 권한 부여 완료
- Docker 컨테이너 3개 정상 가동 (zookeeper, kafka, connect)
- kafka-ui는 docker-compose에 있었으나 실행 안 됨 (확인 필요)
- 커넥터 등록/삭제 여러 차례 반복:
  1. 첫 시도: archivelog 미설정 → FAILED
  2. 재시작 후: 전체 3,468 테이블 스키마 스냅샷 → SCN timestamp 에러로 FAILED
  3. store.only.captured 옵션 추가 + kafka:9092 → localhost:9092 수정
  4. connect 컨테이너 재시작 후 정상 RUNNING
  5. schema_only 모드로 변경 → 스트리밍 진입 성공!
  6. 그러나 INSERT 캡처 안 됨 — **RAC 2노드 환경** 때문으로 추정
- RAC: PROD1(oracle19cogg01, thread1), prod2(oracle19cogg02, thread2)
- Redo Log: 6그룹, 각 200MB
- debezium 접속 IP: 192.168.50.31 (어느 노드?)
- 현재 커넥터 상태: schema-changes.oracle3 토픽으로 다시 스냅샷 중
- **2/20 09:50 CDC 성공!** — INSERT 실시간 캡처 확인 완료
- 핵심: enable_goldengate_replication=TRUE 필수, Docker 볼륨 리셋 후 initial 모드로 재시작

## 교훈
- Debezium schema history는 captured tables only 옵션 줘도 전체 스키마 한번 읽음
- schema_only 모드: 기존 데이터 스냅샷 skip, 스트리밍 시작 후 변경만 캡처
- Docker 내부 시간은 UTC (KST = UTC+9)
- 커넥터 빠른 삭제/재생성 시 JMX 메트릭 충돌 → connect 컨테이너 재시작 필요
