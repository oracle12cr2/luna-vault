# 2026-03-03 (화)

## OpenClaw 업데이트
- v2026.2.26 → v2026.3.1 업데이트 완료
- 게이트웨이 재시작 완료

## OEM 13c R5 설치 (진행 중)
- 설치 호스트: 192.168.50.56 (2690v4)
- Repository DB: 192.168.50.27 (PROD, 3노드 RAC - oracle19c01~03)
- OEM Home: /cloud13cr5/oem

### 해결한 문제들
1. **ORA-01843 (월 부적합)**: OS `LANG=ko_KR.UTF-8`이 원인 → `export NLS_LANG=AMERICAN_AMERICA.AL32UTF8`로 해결
2. **transx Failed Parameter Validation**: `MGMT_MESSAGES_IDX_01` 인덱스 누락 → 이전 실패한 설치 잔재. SYSMAN 스키마 전체 삭제로 해결
3. **ORA-00844 MEMORY_TARGET vs SGA_MAX_SIZE 충돌**: `memory_target=2G` < `sga_max_size=5.6G` → spfile에서 memory_target reset으로 해결
4. **SYSMAN 스키마 정리**: DROP USER CASCADE + role/synonym 삭제
5. **which 함수 깨짐**: RHEL `/etc/profile.d/which2.sh`에서 정의한 함수가 OEM 서브셸에서 에러 → `unset -f which`

### 추가 해결
6. **public synonym 체크 실패**: 실제로는 MGMT_VIEW 유저의 private synonym (`MGMT_UPGRADEUTIL_PROPS_E`)이 원인 → `DROP USER MGMT_VIEW CASCADE`로 해결
7. **MGMT_ECM_VIEW role** 삭제 완료
8. 잔재 유저 전부 정리 완료 (SYSMAN_RO, SYSMAN_TYPES, MGMT_VIEW 등)

### ORA-01843 재발 (3차 시도에서도 실패)
- NLS_LANG 환경변수 설정만으로는 부족 — OEM 인스톨러가 자체 DB 세션을 열어서 OS 환경변수 무시
- 해결 시도: DB 시스템 레벨에서 NLS 파라미터 강제 설정 (SCOPE=BOTH SID='*')
  - nls_date_format, nls_date_language, nls_language, nls_territory
- 문제 SQL: `target_tables.sql`의 파티션 정의 `VALUES LESS THAN ('01-JAN-1990')`

### 4차 시도 준비
- DB 시스템 레벨 NLS 파라미터 설정 완료 (SCOPE=BOTH SID='*')
- 50.56 인스톨러 환경: `LANG=en_US.UTF-8`, `LC_ALL=en_US.UTF-8`, `NLS_LANG=AMERICAN_AMERICA.AL32UTF8`
- SYSMAN CASCADE 삭제 진행 중 (오브젝트 수천개라 시간 소요)

### 교훈
- OEM 인스톨러(Java)는 NLS_LANG뿐 아니라 OS LANG 환경변수도 따라감 → 둘 다 영문으로 설정 필요
- SYSMAN 잔재 정리 시 확인할 유저 목록: SYSMAN, SYSMAN_MDS, SYSMAN_APM, SYSMAN_OPSS, SYSMAN_BIPLATFORM, SYSMAN_STB, SYSMAN_RO, SYSMAN_TYPES, MGMT_VIEW, SYSMAN_BIP
- prereqkit 로그에서 체크하는 synonym SQL: `table_owner IN ('SYSMAN','SYSMAN_MDS','MGMT_VIEW','SYSMAN_BIP','SYSMAN_APM','SYSMAN_OPSS','SYSMAN_RO')`

### 현재 상태 (23:03)
- 4차 시도 — **Xmanager 끊김으로 프로세스 종료** (java 프로세스 확인 안 됨)
- EM_REPOS_INIT 단계까지 진행됨 (상당히 진행된 상태)
- 다음 시도 시 **사일런트 모드** (-silent -responseFile) 추천
- SYSMAN 스키마 다시 정리 필요

### 다음에 해야 할 것
1. SYSMAN 스키마 정리 (DROP USER CASCADE 등)
2. response file 준비 (사일런트 설치용)
3. 사일런트 모드로 설치 재시도
4. 환경변수: LANG=en_US.UTF-8, LC_ALL=en_US.UTF-8, NLS_LANG=AMERICAN_AMERICA.AL32UTF8

## 포스트그레스 분리 작업 (예정)
- 별도 VM으로 포스트그레스 분리 계획
- VM 준비 중

## 기타
- 동행복권 자동 구매 자동화 관심 있음 — 검토 필요
- 19:06 타이어 펑크 발생 — 외출 중

## 세계 정세
- **미국-이란 전쟁 발발** (3일차, 2026-03-01~)
  - 미국+이스라엘이 이란 공격
  - 트럼프: "4~5주 예상, 더 오래 할 능력 있다"
  - 이란 초등생 148명 사망, 이란 "전쟁범죄" 주장
  - 코스피 -5.6% 폭락, 국제유가 10% 급등, 닛케이 -2.76%

### SGA_manual.sql 스크립트
- PS_MEMORY=16 기준 수동 메모리 관리 스크립트
- SGA 35%, PGA 15% (12~20G 범위)

### DB 파라미터 (PROD)
- processes: 600
- session_cached_cursors: 200
- open_cursors: 300
- job_queue_processes: 40
- aq_tm_processes: 1
- nls_date_language: AMERICAN
