# 5장. 부분 범위 처리 튜닝

> 📖 출처: **개발자를 위한 오라클 SQL 튜닝** (이경오 저)
> 📄 PDF 페이지: 138 ~ 155

---

chapter 5 
부분범위처리튜닝 
시스댐을 개벌하다 보면 조건에 만족하는 모든 데이터를 한 화면에 보여줄 수 없 
는 경우가 빈번합니다. 이러한 상햄l서 조건에 만족히는 모든 데이터를 스캔한 
다면 DBMS에 많은 
부하를 
주게 되는데， 부분 
범위 처 
리Partial Range Proce잉In g를 
통해 
조건에 만족하는데이터 중특정 건수의 데이터만을스캔하여 처리하면 부하를줄 
일 수 있습니다. 이번 조배서는 이 부분 범위 처리를 자세히 알아보고， 부분 범위 
처리 기법을이용하여 최소값/최대값을찾는튜닝 기법과떼이정 처리를하는튜 
닝 기법에 대해 배우겠습니다. 
￡홉. 부분범위 처리 
‘부분 범위 처리Partial Range Process ing ’ 란 대용량의 테이블을 
스캔할 
때 가장 먼저 나오 
는 한 건 또는 한 건 이상(N개)의 행만 가져오는 처리 기법을 말합니다. 아무리 대 
용량의 테이블을 스캔한다고 해도 부분 범위만 가져온다면 성능은 극단적으로 빨 
라지게됩니다. 
5.1.1 부분 범위 처리의 기초 
EMP 테이블을 전체 범위 처리합니다. 
[전체범위처리] 
SELECT * 
FROM EMP; 
5 부분범위처리튜닝- 139

---
<!-- PDF p.139 -->

다음은 EMP 테이블을 부분 범위 처리합니다. WHERE 절에 ROWNUM 조건을 주면 
원하는 건수만 가져올 수 있습니다.EMP 테이블 전체를 스캔하지 않고 
3건만 읽 
으면곧바로결과만출력하게됩니다 
[부분범위처리] 
SELECT * 
FROM EMP 
WHERE ROWNUM <= 3; 
이번에는 EMP 테이블을 전체 범위 처리하여 EMP 테이블 전체를 스캔하고 
EMPNO 기준으로 정렬한 값을출력합니다. 
[전체 범위 처리 + 소트 연산l 
SELECT * 
FROM EMP 
ORDER BY EMPNO; 
다음은 EMP 테이블을 부분 범위 처리하고 EMP 테이블에서 3 건의 데이터를 가 
져온 후 그 
3건을 EMPNO 기준으로 정렬한 결과를 가져옵니다 
{부분 법위 처리 + 소트 연산} 
SELECT * 
FROM EMP 
WHERE ROWNUM <= 3 
ORDER BY EMPNO; 
하지만 이 SQL문을 자세히 분석할 필요가 있습니다. EMP 테이블을 읽다가 
3건 
을 
모두 읽으면 바로 테이블 스캔을 멈추고 그 후 이 3건만으로 정렬을 하는데， 이 
는 SELECT문에서 ORDER BY 연산이 가장 마지막 진행되기 때문입나다. 사용자 
의 의도가테이블전체 데이터를EMPNO 기준으로정렬한상태에서 3 건의 데이 
터만뽑는 것이었다면 데이터의 정합성이 틀린 SQL문이 되고 맙니다 
140 -

---
<!-- PDF p.140 -->

다음과 같이 인라인 뷰와 
WHERE절에 ROWNUM 조건을 결합하면 EMP 테이블을 
EMPNO 기준으로오름차순정렬한값중에서 3건을가져오게 됩니다. 이 SQL 
문은 인텍스구성 여부에 따라부분범위 처리를하기도하고 전체 범위 처리를하 
기도 합니다. 부분 범위 처리가 되기 위해서는 EMPNO를 선두 컬럼으로 갖는 인 
텍스가 존재해야 하고， 해당 인텍스의 앞에서부터 3건만 읽고 스캔을 종료할 수 
있습니다， 이는 인텍스가정렬 상태를항상유지한다는특성을이용한것입니다 
[부분 범위 치리 + 소트 연산 + ROWNUM 조건] 
SELECT * 
FROM 
SELECT * 
FROM EMP 
ORDER BY EMPNO 
WHERE ROWNUM (= 3; 
5.1.2 
위처력의구현 
TB CUST 태이블을 생성합니다. 
[테이블 생성1
………… … 
CREATE TABLE TB CUST 
CUST_ID VARCHAR2(10), -고객ID 
IN_DTM DATE , 
-가입일자 
CONSTRAINT TB_CUST_PK PRIMARY KEY (CUST_ID) 
10만건의 데이터를입력합니다. 
[데이터입력] 
INSERT INTO TB CUST 
5 부분 범위 처리 튜닝 … 141

---
<!-- PDF p.141 -->

SELECT 
LPAD(TO_CHAR(ROWNUM), 18 , ’0’ ), 
SYSDATHTRUNCWBMS_RANDOM. VALUE(l , 999999 ))/24/68/68) 
FROM DUAL CONNECT BY LEVEL (= 188888; 
COMMIT; 
2015년 10월 01 일 이후 데이터 중에서 가장 최근에 들어간 100건을 출력하는 
SQL문입니다. 이 SQL문은 TB_CUST 테이블을 풀 스캔한 후 IN_DTM을 기준 
으로 내렴치순 정렬한 값 중에서 100건을 출력하는 쿼리입니다 결과 집합은 사 
용자가 의도한 대로 나오지만 
I N_DTM으로 구성된 인텍스가 없다면 전체 범위 
처리를하게됩니다 
[전체범위처리] 
SELECT * 
FROM 
SELECT * 
FROM TB CUST 
WHERE IN_DTM ) = TO_DATE(’281 51881888888 ’, 'YYYYMMDDHH24MISS ’ ) 
ORDER BY IN DTM DESC 
WHERE ROWNUM (= 188; 
IN_DTM으로 구성된(또는 IN DTM이 인텍스 컬럼 선두에 있는) 인텍스를 
생성합니다. 
인텍스를 생성하는 조치만으로 이 SQL문은 부분 범위 처리를 하게 됩니다. 즉， 
부분 범위 처리가 작동하는 데 가장 펼수적인 요소는 인텍스 존재 여부이므로 인 
텍스구성에 따라극적인 성능향상을가져올수 있습니다 
[부분 범위 처리를 위한 인텍스 생성1 
-… 
CREATE INDEX TB_CUST_IDX81 ON TB_CUST(IN_DTM ); 
142 -

---
<!-- PDF p.142 -->

E훌. 최대값/초|소값 스캔 튜닝 
5.2.1 최대값/최소값 스캔 튜닝 
앞 징벼l서 강조하였듯이， 인텍스의 주요 특징은 바로 
‘데이터가 정렬된 상태로 저 
장되어 있다’라는 것입니다. 이러한 인텍스의 특정과 부분 범위 처리의 원리가 결 
합되면 최대값과 최소값을 가져올 때 극적인 성능 향상을 이루어 낼 수 있습니다. 
이처럼 인텍스와부분범위 처리 원리를결합시켜 최대값과최소값을가장빠르게 
가져오는 것을 최대값/최소값 스캔 튜닝이라고 
합니다. 
실습 5-1 부분 범위 처리 기법을 이용하여 최대겹l/초|소값 검색하기 
A 실습준비 
A-1 테이블생성 
TB_ORD_DAY 테이블을 
생성합니다. 
CREATE TABLE TB ORD DAY 
ORD_DT VARCHAR2(8), -주문일자 
ORD_NO VARCHAR2(10) -주문번호 
A-2 데이터입력 
TB_ORD_DAY 테이블을 NOLOGGING 모드로 생성합니다. 
ALTER TABLE TB_ORD_DAY NOLOGGING; 
하나의 ORD_DT값을 기준으로 N개의 ORD_NO값을 가지게 되는데， 오라클 
P L!SQL의 FOR 
LOOP문을 사용하여 데이터를 구성합니다. 총 
3 ，650만 건의 데 
이터를입력합니다. 
5 부분 범위 처리 튜닝 - 143

---
<!-- PDF p.143 -->

DECLARE 
TYPE ORD DT TABLE 1S TABLE OF VARCHAR2(8) 
1NDEX BY B1NARY• 1NTEGER; 
ORD DT TAB ORD DT TABLE; 
i B1NARY 1NTEGER 
0; 
BEG1N 
FOR ORD DT LIST 
1N 
SELECT 
TO_CHAR(SYSDATE-ROWNUM , ’ YYYYMMDD' ) ORD _DT 
FROM DUAL 
CONNECT BY 
LEVEL <= 3650 
ORDER BY 1 
LOOP 
i := i + 1; 
ORD_DT_TAB(i ) := 
ORD• DT LIST.ORD DT; 
1NSERT 1NTO TB ORD DAY 
SELECT 
ORD一DT_TAB ( i ) ，
LPAD (TO_CHAR(ROWNUM ), 10, '0 ’ ) 
FROM DUAL CONNECT BY LEVEL <= 10000; 
COMMIT; 
END LOOP; 
END; 
A-3 기본키생성 
데이터를 입력한후 기본키를생성합니다. 
ALTER TABLE TB ORD DAY 
ADD CONSTRA1NT TB ORD DAY PK 
PR1MARY KEY(ORD_DT, ORD_NO); 
144 -

---
<!-- PDF p.144 -->

A-4 통계정보생성 
테이블과 인텍스의 통계정보를 
생성합니다. 
ANALYZE TABLE TB ORD DAY COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
B. 튜닝전상황 
B-1 튜닝전SQL문 
1 
2 
3 
4 
5 
SELECT 
MAX(ORD_NO) MAX_ORD_NO , 
MIN(ORD_NO) MIN_ORD_NO 
FROM TB ORD DAY 
WHERE ORD_DT = TO_CHAR(SYSDATE - 30 , 'YYYYMMDD ‘ ) 
SQL 분석 
2-3번째 줄 현재 일자로부터 30 일 전 일자를 기준으로 
MAX와 MIN 함수를 이용 
하여 최대값과 최소값을 구하고 있습니다. 
SQL 문제점 
인텍스 범위 스캔을 하지 않고 인텍스 양 끝의 최대값과 최소값만 스캔하면 사용 
자의 의도대로 결과를 도출할 수 있습니다. 하지만 해당 SQL문은 TB_ORD 
DAY_PK 인텍스를 인텍스 범위 스캔하게 됩니다. 
수행시간 
0 . 15초 
B-2 실행계획 
Cost 
13282 
Name 
SELECT STATEMENT 
Operation 
10 
0 
5 부분 범위 처리 튜닝 - 145

---
<!-- PDF p.145 -->

10 
Operation 
Name 
SORT AGGREGATE 
2 
INDEX RANGE SCAN 
TB ORD DAY PK 
수행순서(l D 기준) 
2 •
1 • O 
실행계획설명 
10 
설명 
2 
TB_ORD_DAY_PK 인댁스를 인댁스 범위 스캔(I N DEX RANGE SCAN)합니다 
MAX, MIN 값을 각각 구합니다 
o 
S ELECT절의 연산을 수행합니다 
C. 튜닝 
C-1 튜닝후SQL문 
SELECT 
2 
* 
3 
FROM 
4 
5 
SELECT ORD NO MAX ORD NO 
’ 1 ’ DUMMY 
6 
FROM 
7 
8 
SELECT ORD NO 
9 
FROM TB ORD DAY 
10 
WHERE ORD_DT = TO_CHAR(SYSDATE - 30. 
‘ YYYYMMDD ’) 
11 
ORDER BY ORD_NO DESC 
12 
13 
WHERE ROWNUM <= 1 
14 ) A, 
15 ( 
16 
SELECT ORD NO MIN ORD NO. 
'1 ’ DUMMY 
17 
FROM 
18 
19 
SELECT ORD NO 
146 -
Cost 
13282

---
<!-- PDF p.146 -->

20 
FROM TB ORD DAY 
21 
WHERE ORDJ>T = TO_CHAR(SYSDATE - 30 , 'YYYYMMDD’) 
22 
ORDER BY ORD_NO ASC 
23 
24 
WHERE ROWNUM <= 1 
25 ) B 
26 WHERE A. DUMMY = B. DUMMY; 
SQL 분석 
10번째 줄 
WHERE절에 ORD_DT 조건을 주었습니다. PK 구성이 ‘ORD_DT + 
ORD NO’순으로 되어 있으므로 ORD_DT 조건이 ‘=’ 조건으로 들어오는 순간 
ORD_NO를 기준으로 부분 범위 처리가 가능해집니다. 
11 번째 줄 최대값을 가져오기 위해 ORD NO를 
DESC로 정렬합니다. 
13번째 줄 ROWNUM 조건을 
주어 ORD_NO의 최대값 한 건만 가져오게 합니다. 
21 번째 줄WHERE절에 ORD_DT 조건을주었습니다 PK 구성이 ‘ORD_DT + 
ORD_NO’순으로 되어 있으므로 ORD DT조건이 ‘=’ 조건으로 들어오는 순간 
ORD_NO를 기준으로 부분 범위 처리가 
가능해집니다. 
22번째 줄 최소값을 가져오기 위해 ORD_NO를 
ASC로 정렬합니다. 
24번째 줄 ROWNUM 조건을 
주어 ORD_NO의 최소값 한 건만 가져오게 합니다. 
수행시간 
0.01 초(기존0.15초) 
C-2 실행계획 
C 
~뽕 
0 
SELECT STATEMENT 
8 
NESTED LOOPS 
8 
2 
VIEW 
4 
3 
COUNT STOPKEY 
4 
VIEW 
4 
5 부분 범위 처리 튜닝 147

---
<!-- PDF p.147 -->

” 
삐
 
빠
 
% 
뼈
 
n 
‘m 
뼈
 
nF 
O 
m 
5 
Name 
Cost 
T8 ORD DAY PK 
4 
4 
6 
VIEW 
7 
COUNT STOPKEY 
8 
9 
VIEW 
4 
INDEX RANGE SCAN DESCENDING 
T8 ORD DAY PK 
4 
수행순서(ID 기준) 
5 • 4 • 3 • 2 • 9 • 8 • 7 • 6 • 1 • O 
실행계획설명 
설영 
5 
TB_ORD_DAY _PK 인텍스에서 선두 컬럼인 ORD DT를 
‘=’조건으로 주면 2 번째 컬럼인 
ORD NO 의 범위를 앞에서부터 읽습니다 즉， 최소값을 구합니다 
4 
5번에 대한 인라인 뷰(V IEW)입니다 
3 
4번 인라인 뷰에서 한 건만 읽고 스캔을 멈추게(COUNT STOPKEY) 됩니다 
2 
3 번에 대한 인라인 뷰(VIEW)입니다 
9 
T8 ORD DAY PK 인텍스에서 선두 컬럼인 ORD DT를 ‘=‘조건으로 주먼 2 번째 컬럼인 
ORD_NO의 범위를 뒤에서부터 읽습니다 즉， 최대값을 구합니다 
8 
9번에 대한 인라인 뷰(VIEW)입니다 
7 
8변 인라인 뷰에서 한 건만 읽고 스캔을 멈추게(COUNT STOPKEY) 됩니다 
6 
7 번에 대한 인라인 뷰(VIEW)입니다 
최소값과 최대값을 저장하는 
2개의 인라인 뷰를 중접 루프 조인 (NESTED LOOPS)합니다 
o 
SELECT절의 연산을 수행합니다 
1

---
<!-- PDF p.148 -->

페이징처리튜닝 
5.3.1 떼이징 처리 
일반적인 게시판 또는 정보 조회 화면을 생각해 봅시다.100만 건의 데이터가 저 
장된 테이블이 있다고 가정한다면， 한 화면 안에서 100만 건의 모든 데이터를 보 
여 줄수 없고해당화면에 나타낼 수 있는 범위만보여 주면 됩니다. 이런 경우부 
분범위 처리를이용한페이정 처리때ing Processing를하게 됩니다. 
5.3.2 페이정 j:i 2.1 
페이징 처리 튜닝Paging Processing Tun때이란해당페이지에서 보여 주고싶은 
범위만 
인텍스 범위 스캔하여 특정 데이터만 가져오는 일련의 모든 활동을 의미합니다. 
이는 인텍스를 이용하여 해당 범위의 시작으로 간 
후(인텍스 수직 탐색) 원하는 데이 
터만 가져오는 기법(인텍스 범위 스캔)입니다. 부분 범위 처리를 이용한 페이징 처리 
가 되지 않고 모든 범위를 스캔한 후 
특정 데이터만 가져오게 된다면 시스템은 걷 
잡을수없이큰부하를일으커게됩니다 이러한이유로페이정처리튜닝은실무 
에서 매우 중요하고 빈번하게 쓰이는 
튜닝 기법입니다. 
5-2 부분 범위 처리 기법을 이용하여 떼야장 
A‘ 
A-1 테이블생성 
TB STOCK TRD 테이블을 
생성합니다. 
CREATE TABLE TB STOCK TRD 
TRD_NO VARCHAR2(10) , 
거래번호 
STOCK_CD VARCHAR2(6), 
종목코드 
TRD_DTM VARCHAR2(14) , -거래일시 
5 부분 범위 처리 튜닝 149

---
<!-- PDF p.149 -->

TRD_CNT NUMBER(10) , -거래건수 
TRD_AMT NUMBER(10), -거래긍액 
INST_DTM DATE , -입력일시 
INST_ID VARCHAR2(50 ) 
입력자 
A-2 데이터입력 
TB _STOCK_ TRD 테이블을 NOLOGGING 모드로 설정합니다. 
ALTER TAB LE TB_STOCK_TRD NOLOGGING; 
TB_STOCK_TRD 테이블에 1. 000만건의 데이터를입력합나다 
INSERT 1*+ APPEND */ 
INTO TB_STOCK_TRD - APPEND 힌트 사용 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 10, ’0’ ), 
LPAD(TO_CHAR(RNUM ), 6, ’0' ), 
TRD DTM 
TRUNC <DBMS_RANDOM VALUE ( 100, 10000)), 
TRUNC <DBMS_RANDOM VALUE ( 10000, 100000 )) , 
SYSDATE 
’ DBMSEXPERT ’ 
FROM 
SELECT ROWNUM RNUM FROM DUAL CONNECT BY LEVEL (= 2000 
SELECT 
TO_CHAR(SYSDATE - (ROWNUM*1000000) /24/60/60 , ‘ YYYYMMDDHH24MISS ’ ) TRD_DTM 
FROM DUAL CONNECT BY LEVEL (= 5000 
COMMIT; 
A-3 기본키생성 
데이터를입력한후기본키를생성합니다. 
’50 -

---
<!-- PDF p.150 -->

ALTER TABLE TB STOCK TRD 
ADD CONSTRAINT TB STOCK TRD PK 
PRIMARY KEY (TRD_NO); 
A-4 통계정보생성 
테이블과 인텍스의 통계정보를 생성합니다. 
ANALYZE TABLE TB STOCK TRD COMPUTE STATISTICS 
FOR ALL INDEXED COLUMNS SIZE 254; 
B. 튜닝전상황 
B-1 튜닝전SQL문 
SELECT * FROM 
2 
( 
3 
SELECT 
4 
ROWNUII RNUfol. TRD_DTfol. 
TRD_'대T ， TRD....AIIT, 
5 
COUNT(.) OVER() CNT 
6 
FROM 
7 
8 
SELECT 
9 
TRD DTM. TRD CNT . TRD AMT 
10 
FROM TB STOCK TRD A 
11 
WHERE STOCK_CD = ’000001 ’ 
12 
AND TRD_DTM >= TO_CHAR(SYSDATE-365 , 
‘ YYYYMMDDHH24MISS ’) 
13 
ORDER BY TRD_DTM 
14 
15 ) 
16 WHERE RNUM BETWEEN 21 AND 30; 
SQL 분석 
4번째 줄 
ROWNUM을 이용하여 데이터의 순번을 
매겼습니다. 
5번째 줄 
분석 힘수인 
COUNT 함수를사용하여 결과집합의 전체 건수를가져옵니다. 
5 부분 범위 처리 튜닝 - 151

---
<!-- PDF p.151 -->

11 번째 줄STOCK CD 조건을주어 특정 종목의 데이터를가져옵니다. 
12-13번째 줄 최근 일년간의 거래내역을출력하기 위한조건을줍니다. 
16번째줄21 번째부터 30번째 데이터의 결과만가져옵니다. 게시판을예로들면 
2 번째 페이지를 가져오게 됩니다. 
SQL 문제점 
특정 STOCK_CD의 TRD DTM기준최근의 데이터 중에서 21 번째부터 30번 
째의 데이터만 가져오는 SQL입니다. 30건 중 
10건의 데이터만 가져오는데도 불 
구하고적절한인텍스가존재하지 않아서 테이블전체를테이블풀스캔하고정렬 
작업까지 수행한후에야그중에서 10건만을가져오게 됩니다. 즉， 전체 범위 처리 
를 하고 페이정 처리한 매우 비효율적인 SQL 입니다. 이러한 SQL문이 OLTP 환 
경에서 빈번하게 수행된다면 DBMS 전체 성능에 매우 큰 지장을 주게 됩니다. 
수행시간 
2초 
8-2 실행계획 
이α 
10 
Operation 
0 
SELECT STATEMENT 
VIEW 
2 
WINDOW BUFFER 
3 
COUNT 
4 
VIEW 
5 
SORT ORDER BY 
6 
TABLE ACCESS FULL 
수행순서(10 기준) 
6•
5 •
4 •
3 •
2 •
1 • O 
Name 
Cost 
25171 
25171 
25171 
25171 
25171 
TB STOCK TRD 
25170

---
<!-- PDF p.152 -->

실행계획설명 
TB_STOCK_ TRD 테이블을 테이블 물 스캔 (TABLE ACCESS FUL L)합니다 
TRD_DTM 기준으로 정렬합니다 
5번의 결과를 인라인 뷰(VIEW)로 생성합니다 
분석 함수인 COUNT 힘수 연산을 합니다 
분석 힘수의 결과를 버퍼에 저장(W INDOW BUFFER)합니다 
2 번까지의 결과를 인라인 뷰(VIEW)로 생성합니다 
SELECT절의 연산을 수행합니다 
설명 
10 
4 
6 
5 
3 
2 
0 
C. 튜닝 
C-1 인댁스구성 
부분 범위 처리를 위해서 STOCK_CD와 TRD_DTM으로 구성된 복합 인텍스 
를생성합니다 
CREATE INDEX 
T8 STOCK TRD IDX01 
ON TB_STOCK_TRD(STOCK_CD. TRD_DTM DESC); 
C-2 통계정보생성 
생성한 인텍스의 통계정보를 
생성합니다. 
ANALYZE INDEX TB STOCK TRD IDX01 COMPUTE STATISTICS; 
C-3 튜닝후SQL문 
•
1 
「
4
14 
A
냐
 
ζ
」
SELECT * FROM 
SELECT 
ROWNUM RNUM. TRD_DTM. TRD_CNT, TRD_AMT, 
COUNT(*) OVER() CNT 
5 부분 범위 처리 튜닝 -153

---
<!-- PDF p.153 -->

6 
FROM 
7 
8 
SELECT 
9 
TRD DTM _ TRD C NT _ TRD AMT 
1 ø 
FROM TB STOC K TRD 
11 
WHERE STOCK CD 
’000001 ’ 
12 
AND TRD_DTM >= TO_CHAR(SYSDATE-365. ‘YYYYMMDDHH24MISS ‘) 
13 
ORDER BY TRD DTM 
14 ) 
15 WHERE ROWNUM ( = 30 
16 ) 
17 WHERE RNUM >= 21 
SQL 분석 
11-13번째 줄 
STO CK_CD와 
T RD_DTM으로 구성된 TB_STOCK_TRD 
IDX01 인텍스를 인텍스 범위 스캔합니다. 인텍스 선두 컬럼인 STOCK_CD가 
‘=’ 조건으로 들어가 있으므로 
2번째 컬럼인 TRD_DTM의 일부분만 인텍스 스캔 
후멈출수있습니다 즉，인텍스를이용한효율적인부분범위 처리가기능합나다. 
15번째 줄 ROWNUM 조건으로 인라인 뷰 내에서 30건만 가져오도록 합니다 TB 
STOCK_TRD 의 결과중30건만부분범위 처리로가져오게 됩니다. 
수행시간 
0 . 1 초(기존2초) 
C-4 실행계획 
10 
0뼈ration 
Name 
ø 
SELECT STATEMENT 
VIEW 
2 
WINDOW BUFFER 
3 
COUNT STOPKEY 
4 
VIEW 
5 
TABLE ACCESS BY INDEX ROWID 
TB STOCK TRD 
154 -
Cost 
4 
4 
4 
4 
4

---
<!-- PDF p.154 -->

Name 
lo 
Operation 
6 
INDEX RANGE SCAN DESCENDING 
TB_STOCK_TRD_IDX01 
3 
수행순서(ID 기준) 
6 • 5 • 4 • 3 • 2 •
1 • O 
실행계획설명 
10 
설명 
6 
TB_STOCK_ TRDJDX01 인댁스를 역순으로 인텍스 범위 스캔(I NDEX RANGE 
SCAN 
DESCENDING )합니다 30건의 데이터민 스캔한 후 더는 스캔하지 않고 멈추게 될니다 
5 
6 번에셔 스캔한 30 건의 리프 블록에 있는 ROWID를 바탕으로 테이블 랜덤 액셰스 
(TABLEACCESS BY INDEX ROWID )를 수행합니다 
4 
5번에 대한 인라인 뷰(V IEW)입니다 
3 
6 번，5번，4번의 연산이 30건의 데이터를 가져오는 순간 멈추게 (COUNT STOPKEY) 됩니다 
2 
분석 힘수의 결과를 버떠에 저장(WINDOW BUFFER)합니다 
2번까지의 결과를 인라인 뷰(VI E W)로 생성합니다‘ 
o 
S ELECT절의 연산을 수행합니다 
5 부분 범위 처리 튜닝 - 155

---
<!-- PDF p.155 -->

156 -