## 2026-04-03
- OpenClaw gateway/Control UI 장애 원인 파악: 게이트웨이 프로세스 자체보다는 Control UI origin/token 설정 문제와 기존 nvm 기반 서비스 경로가 주원인이었음.
- 루나 환경 정리 완료: Node/npm/OpenClaw/Claude Code를 nvm 경로에서 시스템 경로(`/usr/bin/node`, `/usr/bin/npm`, `/usr/bin/openclaw`, `/usr/bin/claude`) 기반으로 전환.
- OpenClaw 업데이트 완료: `2026.4.2`, Claude Code 버전 `2.1.91`, `openclaw update status` 기준 up to date 확인.
- `/root/.bashrc`의 nvm 자동 로드 구문(40~42줄) 비활성화하여 새 셸에서도 시스템 경로가 우선되도록 정리.
- systemd 서비스 `/etc/systemd/system/openclaw-gateway.service`를 새로 작성/정리: `ExecStart=/usr/bin/openclaw gateway run --bind custom --port 18789`, PATH는 `/usr/local/bin:/usr/bin:/bin`, config는 `/root/.openclaw/openclaw.json` 사용.
- `openclaw.json`의 `gateway.controlUi.allowedOrigins`를 다음으로 정리:
  - `https://192.168.50.56:18790`
  - `https://oracle23cr2.asuscomm.com`
  - `http://192.168.50.56:18789`
- 최종 상태 확인: `openclaw gateway status`에서 RPC probe ok, `Listening: 192.168.50.56:18789`, 실행 경로 모두 `/usr/bin/*` 로 정상화.
- 운영 규칙 메모: Control UI 기본 접속 URL은 `https://192.168.50.56:18790`, 외부는 `https://oracle23cr2.asuscomm.com`, fallback으로만 `http://192.168.50.56:18789` 사용. Control UI에는 gateway token 저장 유지.
