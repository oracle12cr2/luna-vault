## 2026-04-04
- OEL9 VM 10대(Python 기본 3.9.25) 대상 Python 3.12 전환 작업 완료.
- 대상 서버: 192.168.50.3, .4, .5, .6, .7, .9, .11, .12, .14, .16
- 적용 내용:
  - `python3.12` 패키지 설치
  - `alternatives` 등록 후 `python3` 기본값을 `/usr/bin/python3.12`로 전환
  - `python3.9`, `python3.12` 병행 유지
- 최종 확인:
  - 전 서버 `python3 --version` = `Python 3.12.12`
  - `alternatives --display python3` = 수동(manual) 모드
  - `sudo dnf repolist` 정상 동작 확인
- 롤백 명령:
  - `sudo alternatives --set python3 /usr/bin/python3.9`
- 참고: kto2005 계정은 원격 VM들에서 `sudo -n` 가능(NOPASSWD)이라 일괄 작업 가능했음.
