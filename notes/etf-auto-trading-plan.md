# ETF 자동매매 시스템 — 유나 AI 판단 통합 계획

> 작성: 2026-03-28 (유나)
> 목표: 현재 수동 승인 방식 → 유나가 지표 분석 + 리스크 판단 → 자동 매매 실행

## 현재 시스템 구조

```
┌─────────────────────────────────────────────────────┐
│  crontab (루나 서버 root)                            │
├─────────────────────────────────────────────────────┤
│  08:25  유나 AI 분석 (NEW)                           │
│  08:30  etf_signal.py --mode open   (중기 시그널)    │
│  08:50  etf_redis_realtime.py       (실시간 수집)    │
│  09:30  daytrading.py --mode buy    (단기 매수)      │
│  */10   daytrading.py --mode stoploss (손절 체크)    │
│  14:50  daytrading.py --mode sell   (단기 청산)      │
│  15:20  etf_signal.py --mode close  (중기 종가)      │
│  15:35  kis_candle.py --type min    (분봉 수집)      │
│  16:00  kis_candle.py --type day    (일봉 수집)      │
│  16:10  kis_investor.py             (투자자동향)     │
│  16:10  daily_pnl_report.py         (일일 PnL)      │
└─────────────────────────────────────────────────────┘
```

### 핵심 스크립트 4개
1. **etf_signal.py** (847줄) — 중기 매매: MA크로스 기반, 분할매수 4단계, 손절/익절
2. **daytrading.py** (481줄) — 단기 매매: 멀티팩터 스코어링, 09:30 매수 → 14:50 청산
3. **multifactor.py** (187줄) — 스코어링: 기술적(40%) + 수급(40%) + 모멘텀(20%)
4. **kis_mock_trader.py** (364줄) — 모의투자 API: 주문/잔고 조회

### 현재 매매 흐름
- **etf_signal.py**: MA(3/15) 크로스 → 자동 매수/매도 (이미 자동)
  - 외국인 5일 중 3일 순매도 → 매수 보류
  - 공포지표 PANIC/FEAR → 매수 보류
  - 분할매수 4단계 (25% × 4회, 3일 간격)
  - 손절 -2%~-10%, 익절 +10%
- **daytrading.py**: 멀티팩터 스코어 70점+ → 매수 (현재 **승인 모드**)
  - 14:50 전량 청산 (현재 **승인 모드**)
  - 손절 -3% 자동 (stoploss, 10분 간격)

### 현재 포트폴리오
- 투자금: 1,000만원 (모의투자)
- 3종목: KODEX레버리지(122630), ACE200(105190), KODEX고배당(279530)
- 현재 평가: 9,774,260원 (-2.26%)

---

## 변경 계획 (5단계)

### 1단계: daytrading.py 자동 전환 (즉시 가능)

**목표**: 승인 모드 → 자동 모드로 cron 변경

**변경 내용**:
```bash
# Before (승인 필요)
30 9  * * 1-5  daytrading.py --mode buy
50 14 * * 1-5  daytrading.py --mode sell

# After (자동 실행)
30 9  * * 1-5  daytrading.py --mode buy --auto
50 14 * * 1-5  daytrading.py --mode sell --auto
```

**리스크**: 
- stoploss(-3%)가 이미 10분마다 돌고 있어서 큰 손실 방어 가능
- daytrading은 당일 매수 → 당일 청산이라 오버나잇 리스크 없음

**판단**: ✅ 바로 적용 가능

---

### 2단계: 유나 AI 사전 분석 리포트 (cron 설정 완료)

**목표**: 매일 08:25 유나가 지표 분석 → 리포트 발송

**현재 상태**: cron job 등록 완료 (다음 월요일 8:25 첫 실행)

**분석 항목**:
1. 보유 현황 (signal_positions.json + KIS 잔고)
2. 종목별 일봉 30일 → MA, RSI, 볼린저밴드 계산
3. 공포지표 (VIX, WTI, 환율, 금)
4. 외국인/기관 매매동향 (10일)
5. 전일 대비 수익률

**리포트 출력**:
```
📊 유나 ETF 분석 리포트 (2026-03-31 월)

🌍 시장: FEAR (VIX:27.4, 환율:1,509)
⚠️ 신규 매수 보류 권장

📈 KODEX레버리지 (122630)
  MA3: 78,500 > MA15: 76,200 → 골든크로스 유지
  RSI: 58.3 (중립)
  BB%: 65% (중간)
  외국인: 5일 중 4일 순매도 🔴 → 매수 보류
  판단: ⬜ 홀드

💰 포트폴리오: 9,774,260원 (-2.26%)
⚠️ 리스크: FEAR 상태, 외국인 매도 우세
🎯 오늘 액션: 홀드 (신규 매수 없음)
```

---

### 3단계: 유나 AI 매매 실행 권한 (신중하게)

**목표**: 분석 후 직접 daytrading.py 실행

**조건부 자동 실행 규칙**:

| 조건 | 매수 | 매도 |
|------|------|------|
| 멀티팩터 스코어 80+ & 공포지표 NORMAL | ✅ 자동 매수 | - |
| 멀티팩터 스코어 70-80 & 외국인 순매수 | ✅ 자동 매수 | - |
| 멀티팩터 스코어 70-80 & 외국인 순매도 | ❌ 보류 | - |
| 일 손실 -3% 이상 | - | ✅ 즉시 전량 매도 |
| 공포지표 EXTREME (VIX>30) | ❌ 매수 금지 | ⚠️ 보유분 검토 |
| 보유 종목 수익 +10% | - | ✅ 자동 익절 |
| 종목별 비중 40% 초과 | ❌ 추가 매수 금지 | - |

**구현 방법**:
- 기존 daytrading.py의 `--auto` 플래그 사용
- 유나 cron에서 조건 판단 후 SSH로 실행:
  ```bash
  sudo bash -c 'cd /root/.openclaw/workspace/etf-backtest && \
    /home/anaconda3/bin/python3 daytrading.py --mode buy --auto'
  ```

**안전장치**:
- 일일 최대 매매 횟수 제한 (매수 1회, 매도 1회)
- 매매 실행 시 반드시 디스코드 알림
- 판단 근거를 memory/YYYY-MM-DD.md에 기록
- 주간 리뷰 (매주 금요일 분석 리포트)

---

### 4단계: etf_signal.py 통합 (중기 매매도 유나 판단)

**목표**: 현재 자동인 etf_signal.py에 유나의 추가 판단 레이어 적용

**현재 문제**:
- etf_signal.py는 이미 자동이지만, MA 크로스만으로 판단
- 시장 상황(매크로)을 더 종합적으로 볼 필요 있음

**추가할 판단**:
1. **매크로 분석**: 미국 시장(S&P500, 나스닥) 전일 종가 확인
2. **섹터 로테이션**: 반도체/2차전지/금융 섹터별 강약
3. **변동성 분석**: 최근 5일 변동폭 vs 20일 평균
4. **상관관계**: 종목 간 상관계수 → 분산 효과 확인

**구현**: etf_signal.py의 결과를 유나가 리뷰하고, 필요 시 오버라이드
- signal.py가 매수 신호 → 유나가 최종 검증 후 실행/보류
- signal.py가 매도 신호 → 즉시 실행 (매도는 빠를수록 좋음)

---

### 5단계: 실전 전환 (모의투자 검증 후)

**전환 조건** (모두 충족 시):
- [ ] 모의투자 1개월 이상 운영
- [ ] 최대 낙폭(MDD) -10% 이내
- [ ] 월 수익률 +2% 이상 (3회 이상)
- [ ] 유나 자동 판단 오류 0건 (심각한 판단 실수 없음)
- [ ] 남궁건님 최종 승인

**전환 작업**:
1. KIS API 키를 실전 키로 교체 (kis_mock_trader.py → kis_trader.py)
2. 계좌번호 변경 (모의 50173951 → 실전)
3. 초기 투자금 입금 (1,000만원)
4. 처음 1주일은 소액(100만원)으로 테스트
5. 문제 없으면 단계적 증액

---

## 실행 로드맵

| 주차 | 작업 | 상태 |
|------|------|------|
| **이번 주 (3/28)** | 1단계: daytrading --auto 전환 | 🟡 남궁건님 확인 후 |
| **다음 주 (3/30~)** | 2단계: 유나 분석 리포트 시작 | ✅ cron 등록 완료 |
| **2주차 (4/7~)** | 3단계: 유나 매매 실행 권한 | 리포트 검증 후 |
| **3주차 (4/14~)** | 4단계: signal.py 통합 | 안정화 후 |
| **5주차~ (4/28~)** | 5단계: 실전 전환 검토 | 조건 충족 시 |

---

---

## 실시간 데이터 활용 방안

### 현재 상태
| 항목 | 상태 | 비고 |
|------|------|------|
| **kis_realtime.py** | ✅ 동작 중 | 평일 08:55 시작, WebSocket 체결+호가 수신 |
| **TB_TICK_DATA** | ✅ 385만건 | 체결가, 거래량, 누적거래량, 체결강도 |
| **TB_ORDERBOOK_SNAP** | ✅ 780만건 | 매수/매도 10호가 |
| **etf_redis_realtime.py** | ❌ 3/12 이후 중단 | Redis 캐시 비어있음, 재시작 필요 |
| **daytrading.py 현재가** | ⚠️ KIS REST API 단건 조회 | 실시간 아님, 호출 시점 가격만 |

### 개선 방안 3가지

#### 방안 A: Oracle DB 직접 조회 (가장 간단)
kis_realtime.py가 이미 TB_TICK_DATA에 실시간 저장 중이므로, daytrading.py에서 직접 조회
```sql
-- 최근 5분 체결 데이터 (거래량 추세, 체결강도)
SELECT CURRENT_PRICE, TRADE_VOL, ACCUM_VOL, TRADE_STRENGTH
FROM stock.TB_TICK_DATA
WHERE STOCK_CODE = :code AND TRADE_DT = TO_CHAR(SYSDATE, 'YYYYMMDD')
ORDER BY TRADE_TM DESC FETCH FIRST 100 ROWS ONLY;

-- 호가 스프레드 (유동성)
SELECT SELL_HOG1, BUY_HOG1, TOTAL_SELL_VOL, TOTAL_BUY_VOL
FROM stock.TB_ORDERBOOK_SNAP
WHERE STOCK_CODE = :code AND SNAP_DT = TO_CHAR(SYSDATE, 'YYYYMMDD')
ORDER BY SNAP_TM DESC FETCH FIRST 1 ROWS ONLY;
```

**장점**: 코드 변경 최소, 이미 데이터 있음
**단점**: DB 부하, 약간의 지연 (배치 INSERT 간격 5초)

#### 방안 B: Redis 캐시 복구 + 실시간 조회
etf_redis_realtime.py를 재시작하여 Redis에 실시간 캐시
```python
# Redis에서 현재가 조회 (1ms 이내)
redis_client.hget(f"etf:current:{stock_code}", "current_price")
redis_client.hget(f"etf:current:{stock_code}", "trade_strength")
redis_client.hget(f"etf:current:{stock_code}", "accum_vol")
```

**장점**: 초고속 조회, DB 부하 없음
**단점**: Redis 프로세스 관리 필요, etf_redis_realtime.py 재시작 필요

#### 방안 C: WebSocket 직접 수신 (daytrading 전용)
daytrading.py 안에서 직접 WebSocket 연결, 실시간 체결 스트림 수신

**장점**: 지연 0, 가장 정확
**단점**: 코드 대폭 변경, WebSocket 연결 관리 복잡

### 추천: 방안 A + B 조합 (단계적)

**1단계 (즉시)**: 방안 A — daytrading.py에 DB 조회 함수 추가
- `get_realtime_data(stock_code)` 함수 추가
- 최근 5분 체결 데이터에서 추출:
  - **VWAP** (거래량 가중 평균 가격)
  - **체결강도** (매수 vs 매도 비율, 100 이상이면 매수 우세)
  - **거래량 추세** (최근 5분 거래량 / 30분 평균 대비)
  - **호가 스프레드** (매수1호가 - 매도1호가)
  - **매수/매도 잔량 비율** (총매수잔량 / 총매도잔량)

**2단계 (1주 후)**: 방안 B — Redis 캐시 복구
- etf_redis_realtime.py 재시작
- daytrading.py에서 Redis 우선 조회, 실패 시 DB 폴백

### 매수/매도 판단에 실시간 데이터 반영

#### 매수 조건 강화
기존: 멀티팩터 스코어 70+ → 매수
변경: 멀티팩터 스코어 70+ **AND** 실시간 조건 충족 → 매수

| 실시간 조건 | 기준 | 이유 |
|-------------|------|------|
| 체결강도 > 100 | 매수세 > 매도세 | 매수 흐름 확인 |
| 5분 거래량 > 30분 평균 ×1.2 | 거래 활발 | 유동성 확보 |
| 매수잔량/매도잔량 > 0.8 | 호가 균형 | 급락 위험 낮음 |
| 스프레드 < 0.3% | 호가 촘촘 | 슬리피지 최소화 |

#### 매도 조건 강화 (손절 정밀화)
기존: 10분마다 잔고 조회 → -3% 이하 매도
변경: DB에서 실시간 체결가 확인 → 더 빠른 손절

```
현재 손절 체크: 10분마다 (cron */10)
개선 후: 1분마다 DB 조회 (또는 Redis 실시간)
```

#### 유나 AI 분석에 실시간 데이터 추가

유나 08:25 분석 시:
- 전일 체결 데이터 분석 (장 마지막 30분 매수/매도 추세)
- 전일 호가 잔량 추세 (장 종료 직전 매수잔량 증가 = 익일 상승 가능성)
- 체결강도 추세 (오후 체결강도 > 오전 = 긍정적)

유나 장중 모니터링 (향후):
- 급등/급락 감지 (5분 내 2% 이상 변동)
- 거래량 폭증 감지 (평소 대비 3배 이상)
- 즉시 알림 → 필요 시 긴급 매도

---

## 모니터링 체크리스트

### 매일 (자동)
- [ ] 08:25 유나 분석 리포트
- [ ] 08:30 signal.py 장 전 시그널
- [ ] 09:30 daytrading 매수 결과
- [ ] 10분마다 stoploss 체크
- [ ] 14:50 daytrading 청산 결과
- [ ] 15:20 signal.py 장 후 시그널
- [ ] 16:10 daily PnL 리포트

### 주간 (유나 작성)
- 주간 수익률 정리
- 매매 판단 복기 (맞은 것 / 틀린 것)
- 전략 파라미터 조정 필요 여부
- 리스크 수준 평가

### 월간
- 월간 성과 보고서
- MDD(최대 낙폭) 확인
- 실전 전환 조건 체크
