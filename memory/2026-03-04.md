# 2026-03-04 메모

## EM 13c Cloud Control 설치
- 서버: 192.168.50.56 (2690v4)
- 설치 경로: /cloud13cr5
- 설치 파일: em13500_linux64.bin + zip 4개
- 응답 파일: /cloud13cr5/em13500.rsp
- 리포지토리 DB: oracle19c01:1521/PROD
- silent 모드 설치 실패 — validation 통과 후 OUI가 실행 안 됨
- VNC 서버 설정 (oracle 계정, :1, 포트 5901, 비밀번호 rlaxodhks)
- GUI 모드로 설치 전환 (남궁건이 직접 VNC로 진행 중)
- 서버 기본 부팅 모드: graphical → multi-user.target 변경

## PostgreSQL
- 별도 VM으로 분리함 (오후에 설정 예정)
- IP/호스트명 아직 미확인
- 버전: PostgreSQL 16 (안정 버전 추천)
- 용도: 카프카 데이터 전환용 + 공부용 (책 주문함)
- Kafka Connect + JDBC Sink Connector 연동 예정

## OpenClaw
- v2026.3.2 업데이트 완료

## EM 13c 설치 에러 & 해결
- ORA-01843 (유효하지 않은 월) 에러 — locale이 ko_KR이라 파티션 정의 '01-JAN-1990' 파싱 실패
- 해결: oracle 계정 .bash_profile에 NLS 환경변수 추가
  - export LANG=en_US.UTF-8
  - export NLS_LANG=AMERICAN_AMERICA.AL32UTF8
  - export NLS_DATE_FORMAT="DD-MON-YYYY HH24:MI:SS"
  - export NLS_DATE_LANGUAGE=AMERICAN
  - export NLS_TIMESTAMP_FORMAT="DD-MON-YYYY HH24:MI:SS.FF"
- 2690v4 시스템 locale도 en_US.UTF-8로 변경 (localectl)
- DB 정리: SYSMAN/SYSMAN_TYPES 삭제 시 ORA-01940 (currently connected) 반복 — SCHEDULER JOB이 재접속하는 문제. 계정 LOCK → DISCONNECT SESSION → DROP 순서 필요
- 남궁건이 직접 DB 정리 완료 (SYSMAN, SYSMAN_TYPES, MGMT 테이블스페이스 모두 삭제됨)

## hosts 파일 관리
- 호스트 서버(50.56) hosts를 전 VM에 동일하게 배포 완료 (16대)
- postgres01 (192.168.50.16) 추가됨

## PostgreSQL VM (postgres01) 설정
- IP: 192.168.50.16, 호스트명: postgres01
- kto2005 계정 생성, SSH 키 인증, sudo NOPASSWD 설정
- oracle 계정 삭제 완료
- 스왑 8GB 설정, /dev/shm 4GB로 변경

## VM 공통 설정
- 전체 VM kto2005 계정 sudo NOPASSWD 설정 완료
- oracle VM들은 kto2005 계정 없음 (oracle 계정만)

## 윈도우 노트북
- IP: 192.168.50.224
- OpenSSH 서버 설치 진행 중
- 리니지M은 그냥 노트북에서 직접 실행하기로 함

## Grafana
- Tablespace 패널 소수점 1자리로 변경 (dashboard uid: 6f1a26360, version 6)

## EM 13c 재설치 진행 중
- DB 정리 완료 후 VNC GUI 모드로 재설치 시작 (12:50경)
- NLS 환경변수 적용된 상태
- 13:37 현재 Step 13/14 Repository Configuration 진행 중 (NLS 에러 없이 통과!)

## 모니터링 확장 요청
- CPU/RAM 사용률 1분 단위 모니터링 원함
- node_exporter 미설치 상태 — 설치 필요
- Prometheus: http://localhost:9093 (50.56)
- 현재 타겟: oracledb_exporter(9161), redis_exporter(9121)
- 설치 범위 미확정 (DB만? 전체 VM?)

## SSH 키 최적화
- oracle@192.168.50.56 직접 SSH 키 접속 설정 완료 (sudo su 우회 불필요)

## 동행복권 자동 구매 개발
- 동행복권(dhlottery.co.kr) 매주 자동 구매 시스템 개발 중
- 계정: kto2005 / kto8520!@#
- 예치금 충전 완료
- 환경: Selenium + Google Chrome (headless) on 2690v4
- Playwright는 EPIPE 에러로 사용 불가 (Node.js v24 호환 문제)
- 동행복권 로그인 URL 변경되어 폼 구조 재파악 필요
- 스크립트 위치: /root/.openclaw/workspace/lotto/
- 상태: 개발 중 (로그인 폼 구조 파악 단계)

## 데스크탑 (별도 PC)
- 중국산 메인보드 (알리 구매), GTX 1080 Ti
- NVMe 확장 카드 PCIe x16 장착 — 윈도우에서 미인식
- 디버그 LED "AA" = POST 완료, OS 부팅 핸드오프 단계 (보드 정상)
- NVMe 카드 장착 후 부팅 안 됨 — 부팅 순서 충돌 의심
- CPU/보드 모델 미확인

## Xshell 공개키 등록
- 전체 VM + 호스트 서버에 Xshell ed25519 공개키 등록 완료 (18대)
- 키: ssh-ed25519 ...IEvCbJ ed25519 256-030226
- SSH 점프 호스트: 50.56 경유 → VM 접속 (Xshell 프록시 설정)

## OpenClaw 외부 접속
- 내부 접속 성공: 192.168.50.56:18789/control/ (윈도우에서 OK)
- 외부 포트포워딩: 공유기 3030→18789 시도했으나 3030이 공유기 관리용으로 충돌
- 18789 포트포워딩 추가했으나 외부(맥북) 접속 미확인 — 다음에 테스트
- 보안: gateway.auth.password=rlaxodhks, dangerouslyDisableDeviceAuth=true (임시)
- allowedOrigins에 http://oracle23cr2.asuscomm.com 추가됨

## 기타
- kto2005 sudo 비밀번호: rlaxodhks (전 VM 동일)
- 모든 패스워드: rlaxodhks
- /tmp에 이미지 다운로드: 2026-03-04.jpg, 2026-03-04_2.jpg

## 자비스 프로젝트
- 남궁건이 루나를 자비스처럼 만들고 싶어함
- TTS 테스트 성공 (한국어 음성 답변 가능)
- 아이폰 단축어 "루나" 거의 완성:
  - 구조: 텍스트 받아쓰기 → JSON 텍스트 → POST API 호출 → 음성 말하기
  - API: http://oracle23cr2.asuscomm.com:18789/v1/chat/completions (chatCompletions 활성화됨)
  - 마이크 권한 문제로 테스트 미완 → 내일 이어서
- gateway.http.endpoints.chatCompletions.enabled = true 설정됨
