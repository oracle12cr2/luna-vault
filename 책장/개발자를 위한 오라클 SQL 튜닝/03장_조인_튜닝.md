# 3장. 조인 튜닝

> 📖 출처: **개발자를 위한 오라클 SQL 튜닝** (이경오 저)
> 📄 PDF 페이지: 56 ~ 113

---

C빼 
후이E 니 
-'-L.! TTO 
DBMS 환경에서 시스댐을 개발할 때 펼연적으로 테이블 간의 조인이 필요하게 
됩니다 ‘조인join’이란 2개 이상의 태이블에서 특정 조건에 의해 데이터를 검색하 
는 방법을 말합니다. 오라클에서 제공하는 조인 방식은 크게 3 가지로， 중첩 루프 
조인Nes ted Lo。이010과 
해시 조인Hash ]oin 소트 머지 조인50 r t Merge joi n이 있습니다. 이 
중에서 널리 사용하는방식은중첩 루프조인과해시 조인이며 소트 머지 조인은 
거의 사용하지 않는 
추세입니다. 
이번 징벼1서는 중첩 루프 조인과 해시 조인의 튜닝 기법에 대해 상세하게 알아보 
고 더 나아가 세미 조인Semi )oin 튜닝에 대해서도 살펴보겠습니다. 또한， 실무에서 
복잡한 SQL문에 빈번하게 사용하는 아우터 조인Outer j oin 튜닝에 대해서도 상세 
하게 알아봅니다. 이번장을확실히 이해한다면향후조인SQL문작성 시 성능을 
고려한 
SQL문 
작성이 가능합니다 
훌훌를 중첩 루프 조인 튜닝 
3 ‘ 1.1 중접 루프조인 
중첩 루프 조인Nested Loop join은 프로그래밍에서 중첩 반복문이라고 보면 되는데， 
이 조인은A라는집합을한건한건씩 읽어가면서 해당결과를바탕으료B 집합에 
서 데이터를 찾아가게 됩니다 데이터를 한건 한건씩 찾아나가기 
(이로 인해 테이블 
랜덤 액세스부하가발생) 때문에 대용량 태이블을 중첩 루프 조인한다면 성능에 큰 
부 
3 조인튜닝- 057

---
<!-- PDF p.57 -->

하를주게 됩니다. 하지만그와는반대로효율적인 인텍스스캔을한다면 대용량 
테이블에서도 극적인 성능으로 원히는 데이터를 조회할 수 있습니다(부분 범위 처리 
7}능). 
:iU:II 중칩 루프 조인의 원리 
반복 A 테이블에서 조건에 맞는 행을 스캔 
반복 B 테이블에서 조건에 맞는 행을 스캔 
A 테이블과 B 테이블의 조인 조건이 일치한다면 
해당 행을 결과 집료뻐l 포함 
3.1.20uter 테이블과 Inner 테이블 
‘Outer 테이블’은 중첩 루프 조인에서 가장 먼저 스캔하는 테이블을 
뭇합니다. 다 
른 용어로 ‘Driving 테이블’이라고도 합니다. Outer 테이블의 스캔 건수가 적 
을수록 
중첩 루프 조인 시 유리합나다， 이는 Outer 테이블의 스캔 결과 건수만큼 
Inner 테이블에 대한스캔을반복하기 때문입니다. 
‘Inner 테이블’은 중첩 루프 조인에서 두 번째로 스캔하는 테이블을 돗합니다 다 
른 용어로 ‘Driven 테이블’ 이라고도 합니다. Inner 테이블은 Outer 테이블보 
다 결과 건수가 많아야(즉 Outer 테이블의 결과 건수가 적어얘 중첩 루프 조인 시 유 
리합니다. 또한， Inner 테이블을 스캔할 때 효율적인 인텍스 스캔을 하지 못한 
다면 Outer 테이블의 결과 건수만큼 Inner 테이블을 태이블 풀 스캔하게 되어 
DBMS 전체 시스템에 큰 부히를 주게 됩니다. 
3.1.3 인라인 뷰 
인라인 뷰Inline View란 
FROM절 내에 소괄호 ‘( )’로 감싸져 있는 SELECT문을뭇합 
니다. 인라인 뷰 내에 있는 SELECT문의 결과는 마치 하나의 테이블과 같은 개념 
이 됩니다. 다음의 예제로 자세히 살펴보겠습니다 
058 -

---
<!-- PDF p.58 -->

SELECT * 
FROM 
DEPT A 
) B 
WHERE 
SELECT DEPTNO 
FROM EMP 
A.DEPTNO = B.DEPTNO; 
앞의 예제에서 ‘B’ 로 
감싸져 있는 
SELECT문을 인라인 뷰 내에 존재하는 SELECT 
문이라고 부릅니다. 즉， 집합 
B를 
하나의 테이블(집합)로 취급하게 됩니다. 
이 SQL에서 옵티마이저가 집합 
B를 DEPT 태이블(메인쿼리에 존재)과 같은 레벨 
로 올라가도록 병합하는 것을 ‘View Merging’이라고 부르며 집합 B가 DEPT 
테이블과 같은 레벨로 올라가지 않도록 막는 것을 ‘View N 0 Merging’이라고 
부릅니다. 비용기반 옵티마이저는 일반적으로 인라인 뷰를 View Merging"ð벼 
DEPT와 같은 레벨로 취급하는 특성이 있습니다(쿼리 변환) . 두 개의 테이블이 논 
리적으로 같은 레벨에 존재하면 옵티마이저는 더 많은 접근 경로(Access Path)를 
통해 다양한 실행 계획들을 비교하고 평가하게 되므로 최적의 실행 계획을 도출해 
낼수있습니다. 
3‘ 1 
좋접 루프 조인 튜닝 
다음 
2가지 사항을 지키지 않는 SQL문을 튜닝하는 모든 일련의 활동을 중첩 루 
프조인튜닝이라고합니다. 
• Outer 테이블의 결과 집합이 ξ에f야 합니다 
• Inner 테이블 스캔 시 반드시 효율적인 인텍스 스캔이 이루어져야 합니다 
3 조인튜닝 059

---
<!-- PDF p.59 -->

3.1.5 중첩 루프 조인 튜닝 관련 힌트 
LEADING 
(개설명 
LEADING 힌트는 
2개 이상의 태이블 조인 시 첫 번째로 스캔할 테이블을 지정하 
는힌트입니다. 
(내사용법 
가장 먼저 스캔할 테이블을 지정합니다 
SELECT /*+ LEADING ( 테이블) */ 
(다)여|제 
LEADING 힌트를 이용하여 DEPT 태이블을 선행 테이블로 지정합니다. 이 SQL 
문이 어떤 조인 방식으로 동작할지는 오리클의 용티마이저가 정하게 됩니다 
SELECT /*+ LEADING (A) */ 
* 
FROM DEPT A 
EMP B 
WHERE A.DEPTNO 
10 
AND A. DEPTNO 
B DEPTNO; 
USE NL 
(개설명 
USE_NL 힌트는 중첩 루프 조인을 유도하는 힌트입니다. 해당 힌트의 인자 값은 
Inner 테이블을 지정합나다 
(내사용법 
입력한 태이블을 Inner 태이블로 지정하여 중첩 루프 조인을 
수행합니다. 
060 -

---
<!-- PDF p.60 -->

SELECT /*+ USE_NL ( 테이블) */ 
(다)여|저| 
LEADING 힌트를 이용하여 ACCOUNTS 테이블을 Outer 테이블로 지정하고 
CUSTOMERS 테이블을 Inner 테이블로 지정하여 중첩 루프 조인 연산을 수행 
하게 하는 
SQL입니다. 
SELECT /*+ LEADING (ACCOUNTS) USE_NL(CUSTOMERS ) */ 
ACCOUNTS.BALANCE , 
CUSTOMERS.LAST_NAME , 
CUSTOMERS.FIRST NAME 
FROM 
ACCOUNTS. CUSTOMERS 
WHERE 
ACCOUNTS.CUSTNO = CUSTOMERS.CUSTNO; 
MERGE 
(개설명 
MERGE 힌트는 인라인 뷰로 감싸져 있는 SQL을 메인쿼리와 같은 레벨로 병합하 
는 역할을 합니다. View Merging함으로써 오라클의 옵티마이저는 더 많은 접 
근경로를가지게됩니다. 
(내사용법 
지정한뷰를메인쿼리와같은례벨로합니다. 
SELECT /*+ MERGE(뷰) */ 
(태여|제 
MERGE 힌트를 사용함으로써 인라인 뷰인 
V를 View Merging하여 El 테이블과 
E2 테이블이 같은 레벨이 됩니다. 그 후 오라클의 옵티마이저는 다양한 접근 경로 
3 조인튜닝- 061

---
<!-- PDF p.61 -->

를도출하게됩니다. 
SELECT /성MERG E ( V ) */ 
El.ENAME. El.SAL. V.AVG SAL 
FROM 
EMP El 
(SELECT 
WHERE 
DEPTNO , AVG(SAL ) AVG_SAL 
FROM EMP E2 
GROUP BY DEPTNO) V 
El .DEPTNO = V.DEPTNO AND 
El.SAL > V.AVG SAL; 
NO MERG ε 
(개설명 
NO_MERGE 힌트는인라인뷰로감싸져 있는SQL이 메인쿼리와같은레벨로병합 
되는 것을 방지합니다. No Merging됨으로써 인라인 뷰에 있는 SQL은 독립적 
으로수행됩니다 
(내사용법 
지정한뷰가메인쿼리와같은레벨로병합되는것을방지합니다. 
SELECT ;-.+ NO MERGE ( 뷰) */ 
(다) 여|저| 
NO_MERGE 힌트를 사용함으로써 DALLASDEPT 뷰가 인라인 뷰 내에서 수행되 
도록 합니다. 인라인 뷰인 DALLASDEPT 뷰가 수행 완료되면 DALLASDEPT 
뷰의 결과와El 테이블을조인으로 처리하게 됩니다. 
SELECT /셔N O_M ERGE (DALLASDEPT)*/ 
E1ENAME 
062 -

---
<!-- PDF p.62 -->

DALLASDEPT.DNAME 
FROM 
EMP E1 
(SELECT 
DEPTNO, DNAME 
FROM DEPT 
WHERE 
LOC 
’DALLAS ‘ ) DALLASDEPT 
WHERE 
E1.DEPTNO = DALLASDEPT.DEPTNO; 
실습 3-1 효율적인 중칩 루프 조인으로 결과 도출하기 
A. 실습준비 
A-1 테이블생성 
TB CUST 테이블을 생성합니다. 
CREATE TABLE TB CUST 
CUST_ID VARCHAR2(10), -고객ID 
CUST_NM VARCHAR2(50 ), -고객명 
B1RTH_DT VARCHAR2(8), -생일 
SEX VARCHAR2 (2), 
성별 
PHONE_NO VARCHAR2 (1 1), -폰번호 
J01N_DT VARCHAR2(8), 
가입일자 
1NST_DTM DATE , -입력일시 
1NST_1D VARCHAR2(50) , -입력자1D 
UPDT _DTM DA TE , -수정 
일 시 
UPDT_1D VARCHAR2(50) -
수정자1D 
TB ORD 테이블을 생성합니다 
CREATE TABLE TB_ORD 
ORD_NO VARCHAR2(10), -주문번호 
3 조인튜닝 - 063

---
<!-- PDF p.63 -->

ORD_DT VARCHAR2(8), -주문일자 
ORD_NM VARCHAR2(158), -주문이름 
ORDßMT NUMBER(15), -주문금액 
PRDT_CD VARCHAR2 (6), -상품코드 
SALE_GB VARCHAR2 (2), 一판매구분 
PAY_GB VARCHAR2 (2), 
결제구분 
CUST_1O VARCHAR2 (18), -고객1D 
1NST_DTM DATE , 
입력시간 
1NST_1O VARCHAR2(58 ), -입력자10 
UPDT_DTM DATE , -수정시간 
UPDT 10 VARCHAR2 (5) -수정자10 
A-2 데이터입력 
TB ORD 테이블에 10만건의 데이터를입력합니다. 
1NSERT 1NTO TB CUST 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 18, ’8' ), 
DBMS_RANDOM.STR1NG( ’U ’ , 58 ), 
TO_CHAR(SYSDATE -
TRUNC ( DBMS _RANDOM.VALUE ( 3658 ι 36588 )) , ’YYYYMMDD' ), 
LPAD(MOD(ROWNUM , 2), 2, ‘0‘ ), 
LPAD(TO_CHAR(TRUNC (DBMS_RANDOM VALUE (3658 , 36588 ))) , 11 , ‘0’ ) , 
TO_CHAR(SYSDATE - TRUNC (DBMS_RANDOM.VALUE (l , 365*3 )) , ’ YYYYMMDD ’ ) ι 
SYSDATE 
’DBMSEXPERT ‘ 
NULL 
NULL 
FROM DUAL CONNECT BY LEVEL <= 188888; 
COMMIT; 
DUAL 10 테이블을 생성하여 데이터 복제에 이용합니다. 
CREATE TABLE DUAL 18 
DUMMY VARCHAR2(1) 
064 -

---
<!-- PDF p.64 -->

INSERT INTO DUAL 10 
SELECT DUMMY 
FROM DUAL CONNECT BY LEVEL (= 10; 
COMMIT; 
TB_ORD 테이블을 NOLOGGING 모드로 설정합니다 
ALTER TABLE TB ORD NOLOGGING; 
TB_ORD 테이블에 100만건의 데이터를입력합니다. 
INSERT /*+ APPEND */ INTO TB ORD -APPEND 힌트 사용 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 10, ’O‘), 
TO_CHAR(SYSDATE-TRUNC <DBMS_RANDOM.VALUE(1 , 3650 )) , 'YYYYMMDD' ), 
DBMS RANDOM .STRING(’U' , 150), 
TRUNC <DBMS_RANDOM . VALUE (1000 , 100000)) , 
LPAD(TO_CHAR(MOD(TRUNC <DBMS_RANDOM.VALUE(1, 1000)) , 50 )) , 6, ’0 ’ ), 
LPAD(TO_CHAR(MOD(TRUNC (DBMS_RANDOM.VALUE (1, 1000)) , 3)) , 2, ’0’ ), 
LPAD(TO_CHAR(MOD(TRUNC (DBMS_RANDOM.VALUE (1, 1000)) , 10)) , 2, ’0' ), 
CUST ID 
SYSDATE 
’DBMSEXPERT ’ 
NULL , 
NULL 
FROM TB CUST. DUAL 10; 
COMMIT; 
A-3 기본키생성 
데이터를 입력한후 기본키를생성합니다 
ALTER TABLE TB ORD 
ADD CONSTRAINT TB ORD PK 
PRIMARY KEYWRD NO ); 
AL TER TABLE TB CUST 
3 조인튜닝 - 065

---
<!-- PDF p.65 -->

ADD CONSTRAINT TB CUST PK 
PRIMARY KEY(CUST _ID); 
A-4 외래키생성 
AL TER TABLE TB ORD 
ADD CONSTRAINT TB ORD FK 
FOREIGN KEY (CUST_ID) REFERENCES TB_CUST(CUST_ID); 
• 한 명의 고객은 
0개 혹은 1 개 이상의 주문을 가질 수 있습니다， 
• 하나의 주문은 반드시 한 명의 고객을 가집니다 
A-5 인텍스구성 
TB ORD 테이블에 ‘ORD DT + ORD NM’으로 구성된 복합 인텍스를 생성 
합니다. 
CREATE INDEX TB_ORD_IDX01 ON TB_ORD(ORD_DT , ORD_NM); 
A-6 통계정보생성 
태이블과 인텍스에 대한 
통계정보를 생성합니다 
ANALYZE TABLE TB CUST COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB ORD COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
α
 
υ
 
8-1 튜닝 전SQL문 
SELECT 
2 
1*+ LEADING(A) USE_NL(B) */ 
3 
* 
066

---
<!-- PDF p.66 -->

FROM TB_CUST A, TB_ORD B 
WHERE A.CUST NM LIKE 'L%’ 
AND A.CUST_ID = B.CUST_ID 
AND B.ORD DT BETWEEN 
TO_CHAR(SYSDATE-365 , ‘ YYYYMMDD ‘) 
AND TO_CHAR(SYSDATE, ’YYYYMMDD’); 
4 
5 
6 
7 
8 
9 
SQL 분석 
2번째 줄 오라클의 LEADING 힌트를 이용하여 TB_CUST 태이블을 Outer 테이 
블로 설정합니다. 또한. USE_NL 힌트를 이용하여 TB ORD 테이블과 중첩 루프 
조인이 이루어지게 합니다. 
6번째 줄 TB_CUST 태이블과 TB_ORD 테이블을 
CUST_ID로 ‘=’ 조인하였습 
니다. TB_ORD 테이블에 CUST_ID를 선두 컬럼으로 한 인텍스가 존재하지 않 
으므로TB_ORD 테이블은테이블풀스캔으로처리됩니다. 
SQL 문제점 
TB ORD 테이블에 CUST_ID를 선두 컬럼으로 구성된 인텍스가 존재하지 않습 
니다. 이러한 상햄서는 TB_CUST 테이블의 결과 집합의 건수만큼 TB ORD 
테이블을테이블풀스캔하게 됩나다. 
수행시간 
12분55초 
8-2 실행계획 
Name 
43M 
SELECT STATEMENT 
0 
FILTER 
NETSTED LOOPS 
2 
480 
TB CUST 
TABLE ACCESS FULL 
3 
8617 
TB ORD 
TABLE ACCESS FULL 
4 
067 
3 조인튜닝

---
<!-- PDF p.67 -->

수행순서(ID 기준) 
3 • 4 • 2 •
1 • O 
실행계획설명 
10 
셜명 
3 
TB_CUST 테이블을 테이블 울 스캔(TABLE ACCESS FUL L)합니다 
4 
TB ORD 테이블을 테이블 물 스캔(TABLE ACCESS FULU합니다 
2 
TB_CUST 테이블과 TB_ORD 테이블을 중접 루프 조인 (NESTED LOOPS)합니다 즉， TB 
CUST의 결과 집합의 건수만큼 TB ORD 테이블을 테이블 물 스캔하게 됩니다 
조건 절에 조건에 따른 필터링 처리를 합니다 
o 
SELECl절의 연산을 수행합니다 
C. 
C-1 인텍스구성 
CREATE INDEX TB CUST IDX81 ON TB CUST(CUST NM); 
CREATE INDEX TB_ORD_IDX82 ON TB_ORD(CUST_ID, ORD_DT); 
• Outer 테이블인 TB_CUST 테이블의 효율적인 스캔을 위하여 CUST NM 컬럼으로 
구성된 인댁스를추가하였습니다 
• Inner 테이블의 효율적인 스캔을 위하여 ‘CUSTJD + ORD_DT’ 컬럼으로 구성된 복 
합 인댁스를 생성하였습니다 
C-2 통계정보생성 
신규로 생성한 인텍스에 대한통계정보를 
생성합니다. 
ANALYZE INDEX TB CUST IDX81 COMPUTE STATISTICS; 
ANALYZE INDEX B ORD IDX82 COMPUTE STATISTICS; 
C-3 튜닝후SQL문 
SELECT 
068

---
<!-- PDF p.68 -->

2 
/*+ LEADING(A) INDEX(A TB_CUST _IDXÐ1) 
3 
USUL(B) INDEX(B TB_ORD_IDXÐ2) 
*/ 
4 
5 
FROM TB_CUST A, TB_ORD B 
6 
WHERE A.CUST_NM LIKE 'L%’ 
7 
AND A, CUST_ID = B.CUSLID 
8 
AND B.ORD DT BETWEEN 
9 
TO_CHAR(SYSDATE-365, ‘ YYYYMMDD ‘ ) 
18 AND TO_CHAR(SYSDATE , ‘ YYYYMMDD ’ ); 
SQL 분석 
2-3 번째 줄 LEADING 힌트를 이용하여 TB CUST 테이블을 Outer 테이블로 
지정하였습니다. INDEX 힌트를 사용하여 TB_CUST_IDXOl 인텍스를 스캔하 
도록 하였습니다 해당 인텍스는 ORG NM 컬럼으로 이루어져 효율적인 인텍스 
스캔이가능합니다. 
7번째 줄 USE NL 힌트를 이용함으로써 중첩 루프 조인을 유도하였으며 TB 
ORD 테이블을 Inner 테이블로 지정하였습니다. INDEX 힌트를 사용하여 TB 
ORD IDX02 인텍스를 스캔하도록 하였습니다. 해당 인텍스는 ‘CUST_ID + 
ORD DT’ 컬럼으로 이루어진 복합 인텍스입니다. 해당 인텍스를 사용함으로써 
Outer 테이블인 TB_CUST 테이블의 스캔 건수만큼 Inner 테이블인 TB_ORD 
테이블을스캔시 효율적인 인텍스스캔으로극적인 성능향상이 이루어집니다. 
수행시간 
0.9초(기존 12분55초) 
網
*
빼
敏
 
행
 
O
I 
실
 
• 
”
다
 
·
뼈
-
Name 
Cost 
0 
SELECT STATEMENT 
15792 
FILTER 
2 
NESTED LOOPS 
3 
NESTED LOOPS 
15792 
3 조인튜닝- 069

---
<!-- PDF p.69 -->

Operätìon 
iν 
Name 
Cost 
4 
5 
TABLE ACCESS BY 1NDEX ROW1D 
TB CUST 
3975 
1NDEX RANGE SCAN 
TB CUST IDX01 
37 
6 
1NDEX RANGE SCAN 
TB ORD 1DX02 
2 
7 
TABLE ACCESS BY 1NDEX ROWID 
TB ORD 
3 
수행순서(ID 기준) 
5 •
4 • 6 •
3 •
7 •
2 •
1 • O 
실행계획설명 
10 
설명 
5 
TB_CUST J DX01 을 인텍스 범위 스캔( 1 NDEX RANGE 
SCAN)합니다 
4 
5 번에서 스캔한 ROW1D를 기준으로 TB_CUST 테이블에 테이블 랜덤 액셰스(T AB LE 
ACCESS BY 1NDEX ROW1D)를 수행합니다 
6 
TB_ORDJDX02을 인텍스 범위 스캔(I NDEX RANGE 
SCAN)합니다 
3 
4번과 
6번에 대한 중칩 루프 조인 (NESTED LOOPS) 연산을 수행합니다 
7 
6 번에서 스캔한 RO W 1D를 기준으로 TB ORD 테이블에 테이블 랜덤 액세스(TABLE 
ACCESS BY 1NDEX ROW1D)를 수행합니다 
2 
3 번과 6 번에 대한 중칩 루프 조인(NESTED LOOPS) 연산을 수행합니다 
조인 결과에 대해 조건에 따른 필터링 처리를 합니다 
o 
SELECT절의 연산을 수행합니다 
해시조인튜닝 
3.2.1 해시조인 
두 개의 태이블을 
조인한다고 가정할 
때 작은 집합을 
빠르게 읽어 해시 테이콸10s h 
Ta ble을 생성하여 해시 영역H ash Areaojl 저장한 
후， 큰 테이블을 순차적으로Sequ e nti시 
읽으면서 해시 함수Hash Fu nction에 입력 값을주어 해시 영역에 있는해시 테이블(작 
0/0

---
<!-- PDF p.70 -->

은 집합) 내에 해당 값이 존재하면 조인 집합에 저장하여， 원하는 조인 결과를 도출 
해내는방식입니다. 
일반적으로 대용량 태이블의 조인 연산에서는 해시 조인 방식이 중첩 루프 조인이 
나 소트 머지 조인 방식보다 훨씬 효율적입니다. 즉， 작은 집합과 큰 집합이 있는 
상햄서 
오라클의 해시 조인은 극적인 성능 향상을 이루어 냄니다. 하지만 해시 
조인은 대용량 테이블 조인 시에 메모리가 많이 펼요합니다. 해시 조인의 성능을 
극대화하려면 오라클 DBMS에 대한 메모리 관리도 신경을 써야 
합니다(작은 집합 
을 해시 영역에 담을 빈 메모리 확보). 
3.2.2 해시조인2.1 
해시 조인의 특성은 다음과 같습니다. 
• 
두 개의 테이블 중 한 테이블이 작은 집합이어야 성능 극대화가 가능합니다. 
• 
조인 조건이 반드시 낱 조건이어야 
합니다， 
일반적인 업무에서 두 개의 테이블이 l:M 관계에 놓였을 경우 
1쪽 
집합이 훨씬 작 
은 용량의 테이블인 경우가 많습니다. 이러한 상햄l서 1쪽 집합을 Build Input 
으로하여 해시 조인을수행한다면극적인 성능향상을 이루어 낼수 
있습니다. 
3.2.3 Build Input과 Probe Input 
‘Build Input'은 
해시 조인 시 해시 영역에 저장하는 집합을뭇합니다. 중첩 루프 
조인 기준으로는 Outer 테이블이라고 이해하시면 됩니다. Build Input은 반드 
시 작은집합이어야해시 영역에 메모리 공간을초과하지 않고들어갈수 있습니 
다. Build Input이 지나치게 큰 테이블이 된다면 오히려 메모리 영역과 디스크 
영역 사이에 페이징Paging이 발생하게 되어 성능이 떨어질 위험이 있습니다 
‘Probe Input'은해시 조인 시 해시 영역에 저장된 Build Input의 데이터가해 
3 조인튜닝 071

---
<!-- PDF p.71 -->

시 방식 접근으로 조인을 수행딴 
집합을 돗합니다. 해시 영역에 생성된 해시 테 
이블이 구성되면 Probe Input을 
순차적으로 스캔하면서 해시 함수를 통한 해시 
테이블(Build Input) 검색을 하게 됩나다. 이때 Probe Input은 큰 
용량의 테이블 
을지정하여야합니다. 
3.2.4 해시 조인을 위한 메모리 관리 
오라클의 PGAPrivate Global Area 영역은 해시 조인 시 사용하게 되는 메모리 영역입 
니다. 해당 해시 조인 시 Build Input이 PGA 영역에 모두 담길 정도로 작다면 
최적。plimal 연산이 일어나면서 극적인 성능을 발휘합니다. 하지만 Build Input 
이 너무 커서 PGA 영역에 모두 담지 못하게 되면 임시 공간Temp Space 영역을 이용 
하게 되면서 디스크 
I!ü가 발생하게 됩니다. 즉， 멀티 패스M 니lti Pass 연산이 일어나 
게 됩니다. 이러한 경우에는 PGA_AGGRATE_TARGET 파라미터의 메모리 크기를 
확장하여 성능향상을꾀할수 
있는데， 다음명령어로 이를수행할수 
있습니다. 
ALTER SYSTEM SET PGA_AGGREGATE_TARGET=6G; 
3.2.5 해시조만 
해시 조인 튜닝은 기존의 해시 조인 방식이 아닌 중첩 루프 조인이나 소트 머지 조 
인 방식으로 되어 있는 SQL을 해시 조인 연산 조건에 부합되는지 분석한 후 해시 
조인 방식으로 바꾸는 일련의 모든 활동을 
돗합니다. 
3.2.6 해시 조안 튜닝 관련 힌트 
USE HASH 
(개설명 
USE_HASH 힌트는옹티마이저에 해시 조인을유도합니다 
072 -

---
<!-- PDF p.72 -->

(내사용법 
입력한 두 집합 중 작은 집합을 Build Input으로 지정하여 큰 집합인 Probe 
Input과 
해시 조인을 
수행합니다. 
SELECT /*+ USE_HASH ( 테이블 테이블) */ 
(q) 여|저| 
USE_HASH 힌트 안에 해시 조인이 대상이 되는 테이블을 입력합니다 용티마이저 
는 두 개의 테이블 중 작은 집합을 Build Input으로 하여 해시 조인을 유도하게 
됩니다. 여기서는 DEPT 테이블이 더 작기 때문에 DEPT 테이블을 Build Input 
으로선택하게됩니다. 
SELECT /*+ USE HASH(EMP DEPT)*/ * 
FROM EMP. DEPT 
WHERE EMP.DEPTNO = DEPT.DEPTNO 
LEADING 힌트와 결합하여 Build Input을 직접 지정하는 방법도 있습니다. 가장 
널리쓰이는방식은다음과같습나다. 
DEPT 테이블을 선행 테이블로 삼고 EMP 테이블을 Probe Input으로 합니다 
즉， 선행 테이블인 DEPT 태이블이 Build Input이 되고 EMP 테이블은 Probe 
Input이 됩니다. 
SELECT /*+ LEADING(DEPT) USE .꺼ASH(EMP)*/ * 
FROM EMP, DEPT 
WHERE EMP .DEPTNO = DEPT .DEPTNO; 
3 조인 튜닝 - 073

---
<!-- PDF p.73 -->

실습 3-2 해시 조인으로 성능 극대화하기 
A.실습준비 
A-1 테이블생성 
TB PRDT 테이블을 생성합니다. 
CREATE TABLE TB PRDT 
PRDT_CD VARCHAR2 (6), -상품코드 
PRDT_NM VARCHAR2(50 ), -상품명 
REL_DT VARCHAR2(8), -출시일자 
COSTßMT NUMBER(15 ), -원가금액 
1NST_DTM DATE , -입력시간 
1NST_ID VARCHAR2(50 ), -입력자1D 
UPDT_DTM DATE , 
수정시간 
UPDT_1D VARCHAR2(5) 
수정자ID 
TB_ORD 테이블을 생성합니다 
CREATE TABLE TB ORD 
ORD_NO VARCHAR2(10) , -주문번호 
ORD_DT VARCHAR2(8) , -주문일자 
ORD_NM VARCHAR2(150), 
주문이름 
ORD_AMT NUMBER(15 ), 
주문금액 
PRDT_CD VARCHAR2(6) , 
상품코드 
SALE_GB VARCHAR2 (2), -판매구분 
PAY_GB VARCHAR2(2), -결제구분 
CUST_ID VARCHAR2(10), -고객ID 
1NST_DTM DATE , -입력시간 
INST_ID VARCHAR2 (50 ), -입력자ID 
UPDT_DTM DATE , -수정시간 
UPDT ID VARCHAR2 (5) -수정자1D 
074 -

---
<!-- PDF p.74 -->

A-2 데이터입력 
TB PRDT 태이블에 10만 건의 데이터를 입력합니다. 
INSERT INTO TB PRDT 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 6, ’O‘ ), 
DBMS_RANDOM ‘ STRING( ’U’ , 50 ), 
TO_CHAR(SYSDATE - TRUNCWBMS_RANDOM.VALUE (1, 3650 )) , ‘ YYYYMMDD ’ ), 
TRUNC(DBMS_RANDOM. VALUE ( 100 , 100000)) , 
SYSDATE 
‘ DBMSEXPERT ’ 
NULL 
NULL 
FROM DUAL CONNECT BY LEVEL (= 100000; 
COMMIT; 
DUAL 10 테이블을 생성하여 데이터 복제에 이용합니다. 
CREATE TABLE DUAL 10 
DUMMY VARCHAR2(1) 
INSERT INTO DUAL 10 
SELECT DUMMY 
FROM DUAL CONNECT BY LEVEL ( = 10; 
COMMIT; 
TB_ORD 테이블을 NOLOGGING 모드로 설정합니다 
ALTER TABLE TB ORD NOLOGGING; 
TB_ORD 테이블에 500만건(1 0x500000)의 데이터를입력합나다. 
INSERT /*+ APPEND */ 
INTO TB ORD -APPEND 힌트 사용 
SELECT 
3 조인튜닝 - 075

---
<!-- PDF p.75 -->

LPAD(TO_CHAR(ROWNUM ), 10 , ’0’ ), 
TO_CHAR(SYSDATE-TRUNC <DBMS_RANDOM . VALUE ( 1, 3650 )) , ’ YYYYMMDD ‘ ), 
DBMS RANDOM .STRING (‘U‘ , 150), 
TRUNC <DBMS_RANDOM. VALUE (1000, 108(08)) , 
LPAD(TO_CHAR(TRUNC (DBMS_RANDOM .VALUE (l , 100(00))) , 6, ’0' ), 
LPAD(TO_CHAR(MOD(TRUNC <DBMS_RANDOM.VALUE (l , 1(08)) , 3)) , 2, '0 ’ ), 
LPAD(TD_CHAR(MOD(TRUNC <DBMS_RANDDM.VALUE (l , 1(00)) , 10) ), 2, '0' ), 
LPAD(TD_CHAR(TRUNC <DBMS_RANDOM .VALUE (l , 100(0))) , 10 , ’0' ), 
SYSDATE 
'DBMSEXPERT ‘ 
NULL 
NULL 
FROM OUAL_1 0, ( SELECT LEVEL FROM DUAL CONNECT BY LEVEL <= 5800(8 ); 
COMMIT; 
A-3 기본키생성 
데이터를 입력한후 기본키를 생성합니다. 
ALTER TABLE TB PRDT 
ADD CONSTRAINT TB PRDT PK 
PRIMARY KEY (PRDT_CD ); 
ALTER TABLE TB ORD 
ADD CONSTRAINT TB ORD PK 
PRIMARY KEY (QRD_NO ); 
A-4 외래키생성 
ALTER TABLE TB ORD 
ADD CONSTRAINT TB ORD FK 
FOREIGN KEY (PRDT_CD ) REFERENCES TB_PRDT(PRDT• CD); 
• 한 개의 제품은 
0개 또는 1 개 이상의 주문을 가질 수 있습니다‘ 
• 하나의 주문은 반드시 한 개의 제품을 가집니다‘ 
07ß -

---
<!-- PDF p.76 -->

A-5 인텍스구성 
TB ORD 테이블에 ORD_DT 컬럼으로 구성된 인텍스를 생성합니다. 
CREATE INDEX TB_ORD_IDX01 ON TB_ORD(ORD_DT); 
A-6통계정보생성 
테이블과 인텍스에 대한통계정보를생성합니다. 
ANALYZE TABLE TB PRDT COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB ORD COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
B. 
SELECT 1*+ LEADING(A) INDEX(A TB_ORD_IDX(1) USE_NL(B) */ 
A.ORD_NO, A.ORD_DT, B.PRDT_CD, B.PRDT_NM 
FROM TB ORD A 
TB PRDT B 
WHERE A.ORD_DT > TO_CHAR(SYSDATE-365 , 
’ YYYYMMDD ‘ ) 
AND APRDT一CD = B.PRDT_CD; 
8-1 튜닝전SQL문 
,, 
「
ι
?
」
A
낙
 
ζ
J
 
ro 
SQL 분석 
1 번째 줄 오라클의 LEADING 힌트를 이용하여 TB_ORD 테이블을 Outer 테이 
블로 지정하며 Outer 테이블 스캔 시 TB_ORD_IDXOl 인텍스를 이용합니다 
또한， USE_NL 힌트를 이용하여 TB]RDT 테이블을 Inner 테이블로 지정하여 
077 
3 조인튜닝 
중첩루프조인이되도록합니다.

---
<!-- PDF p.77 -->

SQL의문제점 
• 대용량의 테이블인 TB_ORD 테이블이 Outer 태이블로 지정되어 매우 큰 성 
능부하가예상됩니다 
. 
• ORD_DT 컬럼 조건만으로는 대량의 행이 나오기 때문에 그에 따른 테이블 랜 
덤 액세스부하가발생합니다. 
수행시간 
39.8초 
8-2 실행계획 
10 
Operatìon 
Name 
Cost 
G 
SELECT STATEMENT 
677815 
NESTED LOOPS 
2 
NESTED LOOPS 
677815 
3 
TABLE ACCESS BY INDEX ROWID 
TB ORD 
413045 
4 
INOEX RANGE SCAN 
TB ORD IDX01 
1160 
5 
INOEX UNIQUE SCAN 
TB PRDT PK 
0 
6 
TABLE ACCESS BY INDEX ROWID 
TB PRDT 
수행순서( I D 기준) 
4 • 3 • 5 • 2 • 6 •
1 • O 
실행계획설명 
10 
설명 
4 
T8_0RDJDX0 1 을 인댁스 범위 스캔( INDEX RANGE 
SCAN)합니다 
3 
TB ORD IDX01 의 ROW ID를 이용하여 T80RD 테이블을 테이블 랜덤 액세스(TABL E 
ACCESS BY INDEX ROWID)합니다 
5 
T8 ORD 테이블의 PRDT_CD 컬럼 값을 이용하여 T8_PRDT_PK 인텍스를 인댁스 유일 
스캔( INDEX UNIQUE 
SCAN)합니다 
2 
3 번과 5 번의 연산을 중접 루프 조인(NESTED LOOPS )합니다 
078 -

---
<!-- PDF p.78 -->

TB_PRDT_PK에서 나온 ROWID를 이용하여 TB_PRDT 테이블을 테이블 랜덤 액셰스 
(TABLE ACCESS BY INDEX ROWID)합니다 
2 번과 6 번의 연산을 중칩 루프 조인(N ESTED LOOPS)합니다 
6 
SELECT절의 언산을 수행합니다 
0 
SELECT /-+ LEADING(B) FULL(B) USE_HASH(A) 
*/ 
A.ORD_NO , A.ORD• DT, B.PRDT_CD , B.PRDT_NM 
FROM TB ORD A 
TB PRDT B 
WHERE A.ORD_DT > TO_CHAR(SYSDATE-365 , 'YYYYMMDD ’ ) 
AND A.PRDT_CD 
B.PRDT_CD; 
C-1 튜닝후SQL문 
,, 
「
ι
「
3
A
낙
 
q
」
r
。
SQL 분석 
1 번째 줄 LEADING 힌트를 이용하여 작은 집합인 TB PRDT 테이블을 Build 
Input으로 지정합니다. 또한. FULL 힌트를 이용하여 Build Input을 테이블 풀 
스캔으로 유도합니다 마지막으로 USE_HASH 힌트를 이용하여 TB_ORD 테이블을 
Probe Input으로 지정하고 해시 조인을 수행하게 합니다. 
수행시간 
4.9초(기존 39.8초) 
C-2 실행계획 
Name 
43935 
43935 
377 
42652 
SELECT STATEMENT 
HASH JOIN 
TABLE ACCESS FULL 
TABLE ACCESS FULL 
0 
2 
TB PRDT 
TB ORD 
079 
3 조인튜닝 
3

---
<!-- PDF p.79 -->

수행순서(ID 기준) 
2 • 3 •
1 • O 
실행계획설명 
10 
섭명 
2 
TB_PRDT 테이블을 테이블 울 스캔(TAB LE ACCESS FULU합니다 
3 
TB ORD 테이블을 테이블 풀 스캔(TABLE ACCESS FULU합니다 
TB PRDT 테이블을 Build Input. TB_ORD 테이블을 Probe 
I nput으로 하여 해시 조인 
(HASH JOIN)을 수행합니다 
o 
TB_PRDT 테이블을 Build Input. TB_ORD 테이블을 Probe Input으로 하여 해시 조인 
(HASH JOIN )을 수행합니다 
실습3-3 인랴얹 
01용한 해시 조인으로 성능 극대화하기 
A. 실습준비 
A-1 테이블생성 
TB PRDT 테이블을 생성합니다 
CREATE TABLE TB PRDT 
PRDT_CD VARCHAR2 (6), 
상움코드 
PRDT_NM VARCHAR2 (50 ), 
상품영 
INST_DTM DATE , 
입력일시 
INST 10 VARCHAR2 (50 ) -입력자 
TB]RDT_SALE_DAY 태이블을 생성합니다. 
CREATE TABLE TB PRDT SALE DAY 
SALE_NO VARCHAR2(10) , -판매번호 
SALE_DT VARCHAR2(8) , -판매일자 
SALE_TM VARCHAR2 (6), -판매시간 
SALE_CNT NUMBER(9), -판매수 
080 -

---
<!-- PDF p.80 -->

SALE_AMT NUMBER(9), -판매긍액 
PRDT_CD VARCHAR2(6), -제품코드 
INST _DTM DATE , -입력일시 
INST_ID VARCHAR2(50 ) -입럭자 
A-2 데이터입력 
TB PRDT 테이블에 10만 건의 데이터를 입력합니다. 
INSERT INTO TB PRDT 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 6, ’0' ), 
DBMS_RANDOM.STRING (‘U‘, 50 ), 
SYSDATE 
'DBMSEXPERT' 
FROM DUAL CONNECT BY LEVEL <= 100000; 
COMMIT; 
TB_PRDT_SALE_DAY 태이블을 NOLOGGING 모드로 설정합니다. 
ALTER TABLE TB PRDT SALE DAY NOLOGGING; 
TB]RDT_SALE_DAY 테이블에 100만 건의 데이터를 입력합니다. 
INSERT 1-*+ APPEND */ 
INTO TB_PRDT_SALE_DAY -APPEND 힌트 사용 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 10, ’0 ’ ), 
TO_CHAR(SYSDATE - DBMS_RANDOM .VALUE (1, 3650 ), ’YYYYMMDD' ) , 
TO_CHAR(SYSDATE - (DBMS_RANDOM.VALUE(1 , 86400 ) / 24 / 60 / 60), 'HH24MISS ’ ), 
TRUNC (DBMS_RANDOM . VALUE (1, 10)) , 
TRUNC (DBMS_RANDOM.VALUE(1000 , 100000), -3 ), 
PRDT CD 
SYSDATE 
’ DBMSEXPERT ’ 
FROM TB_PRDT , (SELECT DUMMY FROM DUAL CONNECT BY LEVEL <= 10); 
COMMIT; 
3 조인튜닝 - 081

---
<!-- PDF p.81 -->

A-3 기본키생성 
데이터를 입력한후 기본키를생성합니다. 
ALTER TABLE TB PRDT 
ADD CONSTRAINT TB PRDT PK 
PRIMARY KEY (PRDT_CD ); 
ALTER TABLE TB PRDT SALE DAY 
ADD CONSTRAINT TB PRDT SALE DAY PK 
PRIMARY KEY (SALE_NO ); 
A-4 외래키생성 
ALTER TABLE TB PRDT SALE DAY 
ADD CONSTRAINT TB PRDT SALE DAY FK 
FOREIGN KEY (PRDT_CD) REFERENCES TB_PRDT(PRDT_CD ); 
• 한 개의 제품은 
0개 혹은 1 개 이상으| 판매내역을 가질 수 있습니다 
• 하나의 판매내역은 반드시 한 개의 제품을 가집니다 
A-5 통계정보생성 
테이블과 인텍스에 대한 통계정보를 생성합니다 
ANALYZE TABLE TB PRDT COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB PRDT SALE DAY COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
B‘튜닝전상황 
8-1 튜닝전SQL문 
SELECT 
2 
B.PRDT CD 
3 
MIN (B.PRDT_NM), 
082 -

---
<!-- PDF p.82 -->

4 
SUM(A.SALE_CNT), 
5 
SUM(A .SALE_AMT) 
6 
FROM 
7 
T8 PRDT SALE DAY A. T8 PRDT 8 
8 
WHERE 
9 
A.SALE DT BETWEEN 
'201201 이 ’ AND ‘20131231' AND 
10 
A.PRDT CD 
B.PRDT CD 
11 
GROUP BY B.PRDTCD; 
SQL 분석 
7번째 줄 TB]RDT_SALE_DAY 테이블과 TB]RDT 테이블을 조인합니다 
조인 방식은 옵티마이저의 선택에 따릅니다. 
SQL의문제점 
TB_PRDT 테이블과 TB PRDT SALE DAY 테이블은 l:M 관계입니다 1쪽 
집합인 TB]RDT 테이블의 PRDT_CD를 기준으로 GROUP 
BYõ때 집계 결과 
를 계산하고 있습니다. 이러한 SQL은 인라인 뷰를 이용하여 M쪽 집합인 TB 
PRDT_SALE_DAY 테이블을 먼저 GROUP 
BY한 후 TB]RDT 태이블과 1:1 
관계를 만든 다음 조인 연산을 수행한다면 최적의 성능을 낼 수 있습니다. 
수행시간 
1. 2초 
8-2 실행계획 
10 
Operation 
0 
2 
SELECT STATEMENT 
HASH GROUP BY 
HASH JOIN 
3 
4 
TABLE ACCESS FULL 
TABLE ACCESS FULL 
Name 
TB PRDT SALE DAY 
TB PRDT 
Cost 
2845 
2845 
2844 
2500 
343 
3 조인튜닝 - 083

---
<!-- PDF p.83 -->

수행순서(10 기준) 
3 • 4 • 2 •
1 • O 
실행계획설명 
10 
설명 
3 
TB_PROT _SALE_OAY 테이블을 툴 스캔(TABLE ACCESS 
FUL L)합니다 
4 
TB_PROT 테이블을 풀 스캔(TABLE ACCESS 
FUL L)합니다 
2 
4번과 5 번을 해시 조인 (HASH JOIN )합니다 
GROUP BY 연산을 수행합니디 
o 
SELECT절의 연산을 수행합니다 
c‘ 
C-1 튜닝후SQL문 
SELECT 
/*+ NO_MERGE(A) */ 
2 
BPRDT CD 
3 
BPRDT NM 
4 
A.SALE CNT SUM 
5 
A.SALE AMT SUM 
6 
FROM 
7 
( 
8 
SELECT 
9 
A.PRDT_CD , 
10 
SUM(A.SALE_CNT) SALE_CNLSUM , 
11 
SUM(A.SALE~MT) SALE~MT_SUM 
12 
FROM TB PRDT SALE DAY A 
13 
WHERE 
14 
A.SALE_DT BETWEEN ‘ 20120101 ‘ AND ’20131231 ’ 
15 
GROUP BY A.PRDT_CD 
16 ) A, 
17 TB PRDT B 
18 WHERE 
19 A.PRDT (0 
B.PRDT (0; 
084

---
<!-- PDF p.84 -->

SQL 분석 
1 번째 줄 NO_MERGE 힌트를 이용하여 인라인 뷰 A가 메인쿼리와 같은 레벨로 
View Merging되지 않게 합니다. 
7-16번째 줄 TB]RDT _SALE_DAY 테이블을 인라인 뷰 내에서 읽습니다. 
PRDT_CD 기준으로 GROUP 
BY하여 TB]RDT 테이블과 조인 연산을 최소화 
합니다. 
수행시간 
0.8초(기존1.2초) 
C-2 실행계획 
10 
Operation 
0 
SELECT STATEMENT 
HASH JOIN 
2 
VIEW 
3 
HASH GROUP BY 
4 
TABLE ACCESS FULL 
TB PRDT SALE DAY 
5 
TABLE ACCESS FULL 
TB PRDT 
Cost 
2845 
2845 
2501 
2501 
2500 
343 
nU 
쥔
 
F ” 
이
 
• 
이
 
2 
원
 
3 
팽
 
ι
언
 
실행계획설명 
10 
설명 
4 
TB_PRDT _SALCDAY 테이블을 풀 스캔(TABLE ACCESS FUL L)합니다 
3 
GROUP BY 연산을 합니다 
2 
4번과 3 번의 연산을 인라인 뷰(VIEW)로 구성합니다 
5 
TB_PRDT 테이블을 테이블 울 스캔(TABLE ACCESS FUL L)합니다 
2 번과 
5번을 해시 조인 (HASH JOIN)합니다 
o 
SELECT절의 연산을 수행합니다 
3 조인튜닝- 085

---
<!-- PDF p.85 -->

세미조인튜닝 
3.3.1 서|미 조인 
세미 조인Sem i./ oin이란 조인 시 특정 조건에 부합된다면 더는 연산을 수행하지 않는 
것을 돗합니다 SQL문에서 ‘EXISTS’ 또는 ‘NOT 
EXISTS ’를 시용하면 옵티마이 
저는세미 조인 사용여부를판단하게 될니다. 세미 조인은조인 방식에 따라중첩 
루프 세미 조인과 해시 세미 조인으로 나누어집니다. 특정 조건에 맞으면 더는 반 
복하지 않고 멈추기 때문에 성능상매우유리합니다 
3.3.2 EXISTS문과 NOT EXISTS문 
EXISTS문과 NOT 
EXISTS문은 각각 특정 데이터가 존재하거나 존재하지 않는 
데이터를 추출할 때 사용합니다 세미 조인은 이 두 가지 유형의 SQL로 처리할 
때 유용하게 사용할 수 있는데， 세미 조인 유형별로 SQL문을 정리하면 [표 3- 1] 
과같습니다. 
H3-1 세미 조인 유형별 SQL문 
‘ 
-
- - - - -
조인유형 
SQL문 
힌트 
중접루프세미조인 
EXISTS 
NL SJ 
해시세미조인 
EXISTS 
HASH SJ 
중접루프안티 세미 조인 
NOT EXISTS 
NL AJ 
해시안티세미조인 
NOT EXISTS 
HASH AJ 
3.3.3 서|미조인튜닝 
세미 조인 튜닝은 다음의 2가지 방법으로 가장 많이 사용됩니다 
. 
• EXISTS 또는 NOT EXISTS 문이 사용된 SQL을 세미 조인으로 유도합니다 
• UNION 또는 MINUS 집합연산자가사용된 SQL문을세미 조인으로유도합니다 
086 -

---
<!-- PDF p.86 -->

3.3.4 서브쿼리 Unnesting 
서브쿼리는 소괄호 ‘ ( ) ’로 감싸져 있습니다. 옵티마이저는 ‘ ( )’로 감싸진 서브 
쿼리를 중첩되어Nes [Îng 있다고 판단합니다. 중첩된 서브쿼리를 풀어서 메인쿼리 
와 똑같은 레벨로 위치하게 하는 작업을 
‘서브쿼리 Unnesing’이라고 부릅니다. 
즉， 옵티n에저는 서브쿼리를 메인쿼리와 똑같은 레벨로 위치하게 하는 쿼리 변 
환을 수행하게 됩니다. 옵티마이저가 서브쿼리 Unnesting하는 이유는 메인쿼 
리의 테이블과 서브쿼리의 테이블을 같은 례벨로 위치시키변 더 많은 접근 경로 
를 통한 다양한 실행 계획을 도출할 수 있기 때문입니다. 이와 반대로 서브쿼리 
Unnesing을 
하지 않게 하여 무조건 필터 조건으로 서브쿼리의 연산이 처리되게 
하는 것을 ‘서브쿼리 No Unnesting’이라고 부릅니다. 
3.3.5 서|며 조인 튜닝 관련 힌트 
NL_SJ 오~ HASH SJ 
(개설명 
EXISTS문을 쓴 서브쿼리를 사용할 경우 해당 힌트를 사용할 수 있습니다. NL_SJ 
힌트는중첩 루프세미 조인을， HASH_SJ는해시 세미 조인을유도합니다. 
(내사용법 
해당서브쿼리를세미 조인으로유도합니다. 
SELECT /*+ NL SJ 또는 HASH SJ */ 
(다)여|제 
서브쿼리가 
Unnesting된 후 메인쿼리의 테이블인 DEPT 태이블과 서브쿼리의 
테이블인 EMP 테이블을 해시 세미 조인으로 유도합니다 
SELECT * 
3 조인튜닝- 087

---
<!-- PDF p.87 -->

FROM DEPT 
WHERE EXISTS 
(SELECT /*+ HASH SJ*/ * 
FROM EMP 
WHERE EMP .DEPTNO 
DEPT .DEPTNO 
AND SAL > 288(88 ); 
NL_AJ와 HASH_AJ 
(가)설명 
NOT 
EXISTS문을 쓴 서브쿼리를 시용할 경우 해당 힌트를 사용할 수 있습니다. 
NL_AJ 힌트는 
중첩 루프 안티 세미 조인Nested Loop Anri Semi )oin을， HASH_SJ 는 
해시 
안티 세미 조인H빼Anti Semi )oin을유도합니다. 
(내사용법 
해당 서브쿼리를 안티 세미 조인으로 유도합니다 
SELECT /*+ NL AJ 또는 HASH AJ */ 
(다)예제 
서브쿼리가 
Unnesting된 후 메인쿼리의 테이블인 DEPT 테이블과 서브쿼리의 
테이블인 EMP 테이블을 해시 안티 세미 조인으로 유도합니다. 
SELECT * 
FROM DEPT 
WHERE NOT EXISTS 
(SELECT /*+ HASH AJ*/ * 
FROM EMP 
088 -
WHERE EMP .DEPTNO 
DEPT .DEPTNO 
AND SAL > 288(88);

---
<!-- PDF p.88 -->

UNNEST 
(개설명 
UNNEST 힌트는 서브쿼리 Unne sting을 유도하는 힌트입니다 서브쿼리의 내용 
을 메인쿼리와 같은 레벨로 위치하게 합니다 이로써 옵티미어저는 더 많은 접근 
경로를가지게됩니다. 
(내사용법 
해당 서브쿼리를 
Unnesting합니다. 
SELECT / *+ UNNEST */ 
(대여|저| 
UNNEST 힌트를 사용하여 서브쿼리 Unnesting을 유도합니다. 이제 옵티마이저 
는더 많은접근경로를가지게 되어 더 다양한실행 계획을평가하게 됩니다. 
SELECT * 
FROM EMP A 
WHERE A.SAL > 2000 
AND EXISTS 
(SELECT /*+ UNNEST */ '1 ’ 
FROM DEPT B 
WHERE B.DEPTNO = A.DEPTNO 
AND B.LOC LIKE 'C%' ); 
NO UNNEST 
(개설명 
NO_UNNEST 힌트는 서브쿼리 Unnesting을 방지하는 힌트입니다. 해당 힌트를 
사용함으로써 오라클의 옵티마이저는 메인쿼리를 읽으면서 서브쿼리의 조건을 
필터처리하게됩니다. 
3 조인튜닝 089

---
<!-- PDF p.89 -->

(나)사용법 
해당 서브쿼리의 Unnesting을 
방지합니다. 
SELECT /*+ NO UNNEST */ 
(다) 여|저| 
NO_UNNEST 힌트를 사용하여 서브쿼리 Unnesting을 방지합니다.EMP 테이블 
을 읽으면서 EXSISTS문의 조건을필터 처리하게 됩니다. 
SELECT * 
FROM EMP A 
WHERE A.SAL > 2888 
AND EXISTS 
(SELECT /*+ NO_UNNEST */ ’ l' 
FROM DEPT B 
WHERE B.DEPTNO = A.DEPTNO 
AND B.LOC LIKE ’C%’ ); 
실습 3-4 세미 조인 기법을 이용하여 성능 극대화하기 
A.실습춘비 
A-1 테이블생성 
TB_CUST 테이블을 생성합니다. 
CREATE TABLE TB CUST 
CUST_ID VARCHAR2(18), -고객ID 
CUST_NM VARCHAR2(58) -고객명 
TB_CUST _DTL 테이블을 생성합니다 
CREATE TABLE TB CUST DTL 
090 -

---
<!-- PDF p.90 -->

CUST _ID VARCHAR2 (10 ), 
고객ID 
SEQ 
NUMBER ( 3 ) ，-시뭔스 
CUST 1NFO VARCHAR2(150) -고객정보 
TB_ORD 테이블을생성합니다 
CREATE TABLE TB ORD 
ORD_NO VARCHAR2(10) , 
주문번호 
ORD_DT VARCHAR2(8) , -주문일자 
CUST_ID VARCHAR2(10) 
고객1D 
A-2 데이터입력 
TB CUST 테이블에 10만 건의 데이터를 입력합니다. 
1NSERT 1NTO TB CUST 
SELECT 
LPAD(TO_CHAR(ROWNUM), 10, ’0’ ), 
DBMS_RANDOM.STR1NG( ’U‘, 50) 
FROM DUAL CONNECT BY LEVEL (= 100000; 
COMMIT; 
TB_CUST_DTL 테이블에 100만 건의 데이터를 입력합니다. 
INSERT INTO TB CUST DTL 
SELECT 
CUST 1D 
ROW_NUMBER() OVER (PARTIT10N BY CUST_1D ORDER BY CUST_1D), 
DBMS_RANDOM.STR1NG( ‘ Uη 50) 
FROM TB_CUST, ( SELECT LEVEL LV FROM DUAL CONNECT BY LEVEL (= 10); 
COMMIT; 
3 조인튜닝 091

---
<!-- PDF p.91 -->

TB_ORD 테이블을 NOLOGGING 모드로 설정합니다 
ALTER TABLE TB_ORD NOLOGGING; 
TB_ORD 테이블에 500만 건의 데이터를 입력합나다. 
INSERT /*+ APPEND */ INTO TB_ORD -APPEND 힌트 사용 
SELECT 
LPAD(TO_CHAR(ROWNUM), 10 , ’0’ ), 
TO_CHAR(SYSDATE - TRUNCWBMS_RANDOM .VALUE(1, 3650 )) , ‘YYYYMMDD ‘L 
CUST 10 
FROM TB_CUST , ( SELECT LEVEL LV FROM DUAL CONNECT BY LEVEL <= 50 ); 
COMMIT; 
A-3 기본키생성 
데이터를 입력한후 기본커를생성합니다. 
ALTER TABLE TB CUST 
ADD CONSTRAINT TB CUST PK 
PRIMARY KEY (CUST 10); 
ALTER TABLE TB CUST DTL 
ADD CONSTRAINT TB CUST DTL PK 
PRIMARY KEY (CUST_ID , SEQ); 
ALTER TABLE TB ORD 
ADD CONSTRAINT TB ORD PK 
PRIMARY KEY (ORD NO ); 
A-4 인댁스구성 
TB_ORD 테이블에 CUST ID 컬럼으로 구성된 인텍스를 생성합니다. 
CREATE INDEX TB_ORD_IDX01 ON TB_ORD (CUST_ID); 
092 -

---
<!-- PDF p.92 -->

A-5 통계정보생성 
테이블과 인텍스에 대한 통계정보를 
생성합니다. 
ANALYZE TABLE TB CUST COMPUTE STAT1ST1CS 
FOR TABLE FOR ALL 1NDEXES FOR ALL 1NOEXED COLUMNS S1ZE 254; 
ANALYZE TABLE TB CUST DTL COMPUTE STAT1ST1CS 
FOR TABLE FOR ALL 1NDEXES FOR ALL 1NDEXED COLUMNS S1ZE 254; 
ANALYZE TABLE TB ORD COMPUTE STAT1ST1CS 
FOR TABLE FOR ALL 1NDEXES FOR ALL 1NDEXED COLUMNS S1ZE 254; 
B. ,*'r! ~i ~호} 
‘ ïT O 
L-! C그 <0 
8-1 튜닝전SQL문 
SELECT 
2 
/*+ LEADING(A) FULL(A) USE_NL(B) USUL(C) */ 
3 
ACUST ID 
4 
ACUST NM 
5 
BCUST ID 
6 
B.SEQ , 
7 
B.CUST 1NFO 
8 
FROM 
9 
TB CUST A 
10 
TB CUST DTL B 
11 
TB ORD C 
12 WHERE 
13 
A.CUST_NM LIKE 'A%‘ AND 
14 
ACUST ID = BCUST ID AND 
15 
C. CUST ID = B.CUST 1D AND 
16 
C.ORD_DT L1KE ’2015%' 
17 GROUP BY 
18 
ACUST ID 
19 
A.CUST_NM , 
20 
BCUST ID 
21 
B.SEQ , 
22 
B.CUST 1NFO; 
3 조인투닝 - 093

---
<!-- PDF p.93 -->

SQL 분석 
2번째 줄 LEADING 힌트를 이용하여 TB_CUST 태이블을 가장 먼저 스캔하고 해 
당 테이블은 테이블 풀 스캔하도록 하였습니다. 또한， USE_NL 힌트를 이용하여 
TB_CUST_DTL 테이블과 중첩 루프 조인 연산 후 TB_ORD 테이블과 중첩 루 
프 조인하도록 
하였습니다. 
SQL의문제점 
TB ORD 테이블이 가지고 있는 컬럼은 SELECT절에 존재하지 않습니다 TB 
ORD 테이블은 EXISTS 유무만 판단해도 결과 집합에 영향을 주지 않습니다 즉， 
해당테이블은세미조인으로튜닝할수있습니다. 
수행시간 
5.2초 
8-2 실행계획 
ID 
Operation 
0 
SELECT STATEMENT 
HASH GROUP BY 
2 
NESTED LOOPS 
3 
NESTED LOOPS 
4 
NESTED LOOPS 
5 
TABLE ACCESS FULL 
6 
TABLE ACCESS BY INDEX ROWID 
7 
INDEX RANGE SCAN 
8 
INDEX RANGE SCAN 
9 
TABLE ACCESS BY INDEX ROWID 
수행순서(ID 기준) 
5 • 7 • 6 • 4 • 8 • 3 • 9 •
2 •
1 • O 
g잉4 
Name 
Cost 
2678114 
2678114 
2666783 
15284 
TB CUST 
274 
TB CUST DTL 
3 
TB CUST DTL PK 
2 
TB ORD IDX81 
2 
TB ORD 
53

---
<!-- PDF p.94 -->

실행계획설명 
ID 
설명 
5 
TB_CUST 테이블을 테이블 풀 스캔 (TABLE ACCESS FUL L)합니다 
7 
TB_CUST _DTL_PK를 인텍스 범위 스캔( INDEX RANGE SCA N )합니다 
6 
6번에서 잦은 ROWID를 이용하여 TB_CUST _DTL 테이블을 테이블 댄멈 액세스(TABLE 
ACCESS BY INDEX ROWID)합니다 
4 
5번과 
6번의 연산을 중접 루프 조인(N E ST ED L OOPS)합니다 
8 
TB_ORDJDX01 인댁스를 인댁스 범위 스캔(INDEX RANGE 
SCAN)합니다 
3 
4번과 8번의 연산을 중접 루프 조인(NE STED LOOP S)합니다 
9 
3 번에서 나온 TB_ORDJDX01 의 ROWID 값으로 TB_ORD 테이블에 테이블 랜덤 액세스 
(TABLE ACCESS BY INDEX ROWID)합니다 
2 
3 번과 9 번의 연산을 중칩 루므 조인(N E STED LOOP S)합니다 
GROUP BY 연산을 수행합니다 
。
S ELECT절의 언산을 수행합니다 
c. 휴닝 
C-1 튜닝후SQL문 
SELECT 
2 
/*+ LEADING(A) FULL(A) USE_NL(B) */ 
3 
A.CUST_ID , 
4 
ACUST NM 
5 
B.CUST_ID , 
6 
B.SEQ , 
7 
BCUST INFO 
8 
FROM 
9 
TB_CUST A, TB_CUST _DTL B 
10 WHERE 
11 
A.CUST NM LI KE ’A%’ AND 
12 
A.CUST ID = B.CUST ID AND 
13 
EXISTS 
14 
15 
SELECT 
16 
/*+ UNNEST NLSJ INDEX(C TB_ORD_IDXØ1) */ 
17 
’ l' 
3 조인튜닝 - 095

---
<!-- PDF p.95 -->

18 
FROM TB ORD C 
19 
WHERE 
20 
C. CUST ID = B.CUST ID AND 
21 
C.ORD DT LIKE '2015%' 
22 
) ; 
SQL 분석 
2번째 줄 LEADING 힌트를 이용하여 가장 작은 테이블인 TB_CUST 태이블을 
Outer 테이블로 지정하고 FULL 힌트로 Outer 테이블을 테이블 풀 스캔합니다. 
또한， USE_NL 힌트를 사용하여 TB_CUST_DTL을 Inner 테이블로 지정하고 
중첩 루프조인을수행합니다. 
13번째 줄 
EXISTS분을 이용하여 TB ORD 테이블의 존재 여부를확인합니다. 
16번째 줄 NL SJ 힌트를 이용하여 중첩 루프 세미 조인으로 유도합니다 또한， 
INDEX 힌트를이용하여 TB_ORD_IDXOl 인텍스를사용하도록합니다. 
수행시간 
0 .5초(기존 52초) 
C-2 실행계획 
Name 
0 
SELECT STATEMENT 
5453K 
NESTED LOOPS SEMI 
5453K 
2 
NESTED LOOPS 
5126K 
3 
TABLE ACCESS FULL 
TB CUST 
180K 
4 
TABLE ACCESS BY INDEX ROWID 
TB CUST DTL 
680 
5 
INDEX RANGE SCAN 
TB CUST DTL PK 
6 
TABLE ACCESS BY INDEX ROWID 
TB ORD 
3605K 
7 
INDEX RANGE SCAN 
TB ORD IDX01 
OBß

---
<!-- PDF p.96 -->

수행순서(l D 기준) 
3 • 5 • 4 • 2 • 7 • 6 •
1 • O 
실행계획설명 
설명 
3 
TB_CUST 테이블을 테이블 풀 스캔(TABLE ACCESS FUL L)랍니다 
5 
TB_CUST _DTL_PK 인댁스를 인텍스 범위 스캔(I NDEX RANGE 
SCAN)합니다 
4 
5 번에서 나온 ROWID를 이용하여 TB_CUST_DTL 테이블을 테이블 댄덤 액셰스(TABLE 
ACCESS BY INDEX ROWID)합니다 
2 
3 번과 4번의 연산을 중칩 루프 조인 (NESTED LOOPS )합니다 
7 
TB_ORDJDX01 인텍스를 인텍스 범위 스캔(INDEX RANGE SCAN)합LI 다 
6 
7 번에서 나온 ROWID를 이용하여 TB_ORD 테이블을 테이블 랜덤 액셰스(TABLE ACCESS 
BY INDEX ROWID)합니다 
2 번과 6 번의 연산을 중접 루프 세미 조인(NESTED LOOPS SEM I)합니다 
o 
SELECT절의 연산을 수행합니다 
O. 안티세대조인 
EXISTS문을NOT EXISTS로바꾸면 안티 세미 조인이 성립됩니다. 
0-1 SQL문 
SELECT 
2 
/*+ LEADING(A) FULL(A ) USE_NL(B) */ 
3 
A.CUST ID 
4 
ACUST NM 
5 
BCUST ID 
6 
B.SEQ , 
7 
BCUST INFO 
8 
FROM 
9 
TB CUST A. TB CUST DTL B 
18 WHERE 
11 
A.CUST_NM LIKE ’A%' AND 
12 
A. CUST _ID = B .CUST _ID AND 
13 
NOT EXISTS 
14 
(SELECT 
3 조인튜닝 - 097

---
<!-- PDF p.97 -->

15 
/*+ UNNEST NL_SJ INDEX(C TB_ORD_IDX01) */ 
16 
‘1 ’ 
1 7 
FROM TB ORD C 
18 
WHERE 
19 
C. CUST ID = B.CUST ID AND 
28 
C. ORD DT LI KE ’2815%' 
21 
) ; 
SQL 분석 
13번째 줄 NOT 
EXISTS문을 이용하여 TB_CUST 테이블과 TB CUST DTL 
테이블의 조인 결과 중 CUST ID 컬렴을 기준으료 TB ORD 테이블 내에 존재 
하지않는행을검색합니다 
15번째 줄 NL_AJ 힌트를 이용하여 중첩 루프 안티 세미 조인으로 유도합니다. 또 
한， INDEX 힌트를이용하여 TB_ORD_IDXOl 인텍스를사용하도록합니다 
C-2 실행계획 
10 
Operation 
Name 
Cost 
0 
SELECT STATEMENT 
1115872 
NESTED LOOPS ANTI 
1115872 
2 
NESTED LOOPS 
15284 
3 
TABLE ACCESS FULL 
TB CUST 
274 
4 
TABLE ACCESS BY INDEX ROWID 
TB CUST DTL 
3 
5 
INDEX RANGE SCAN 
TB CUST DTL PK 
2 
6 
TABLE ACCESS BY INDEX ROWID 
TB ORD 
22 
7 
INDEX RANGE SCAN 
TB ORD IDX81 
2 
수행순서(l D 기준) 
3 • 5 • 4 • 2 • 7 • 6 •
1 • O 
098 -

---
<!-- PDF p.98 -->

실행계획설명 
10 
설명 
3 
TB_CUST 테이블을 테이블 풀 스캔(TABLE ACCESS FUL L)합니다 
5 
TB_CUST_DTL_PK 인댁스를 인댁스 범위 스캔(INDEX RANGE 
SCAN)합니다 
4 
5 번에서 나온 ROWID를 바탕으로 TB_CUST _DTL 테이블을 테이블 랜덤 액셰스(TABLE 
ACCESS BY INDEX ROWID )합니다 
2 
3 번과 4번의 연산을 중접 루프 조인(N ESTED LOOPS )합니다 
7 
TB_ORDJDXOl 인댁스를 인댁스 범위 스캔(I NDEX RANGE 
SCAN)합니다 
6 
7번에서 나온 ROWID를 이용하여 TB_ORD 테이블을 테이블 랜덤 액셰스(TAB LE ACCESS 
BY I NDEX ROWID)합니다 
2 번과 
6변의 연산을 중칩 루프 안티 세미 조인(NESTED LOOPS ANTI)합니다 
。
SE LECT절의 연산을 수행합니다 
E훌톨 아우터조인튜닝 
3.4.1 아우터조인 
‘아우터 조인。urer Jo in’이란 테이블 
‘A ’와 
‘B ’가 있을 때 A 테이블을 기준으로 B 테 
이블이 조인에 성공하면 B 태이블의 데이터를 보여 주고， 조인에 실패하면 B 테 
이블의 데이터를보여 주지 않는조인 방식입니다. 즉，A 테이블의 내용은조인 성 
공 여부와는상관없이 모두보여 주고B 테이블은조인 성공시에만보여 주는조 
인 방식입니다. 아우터 조인은 다음 
2가지 유형이 있습니다. 
‘Left 아우터 조인’은 왼쪽에 명시한 테이블이 기준이 되는 아우터 조인 방식으로， 
SQL문으로 다음과 같이 표현합니다 여기서 EMP 테이블은 데이터를 모두 보여 
주고 DEPT 테이블은 조인이 성공한 데이터만보여 줍니다. 
SELECT 
* 
FROM EMP LEFT OUTER JOIN DEPT 
3 조인튜닝 - 099

---
<!-- PDF p.99 -->

ON (EMP.DEPTNO = DEPT.DEPTNO); 
‘Right 아우터 조인’은 오른쪽에 명시한 테이블이 기준이 되는 아우터 조인 방식 
으로， SQL문으로 다음과 같이 표현합니다. 여기서 EMP 테이블은 데이터를 모두 
보여 주고 DEPT 테이블은 조인이 성공한 데이터만 보여 줍니다. 
SELECT 
* 
FROM DEPT RIGHT OUTER JOIN EMP 
ON (DEPT.DEPTNO = EMP.DEPTNO); 
이 2가지 아우터 조인 방식을 더 자세히 알아보겠습니다. 
3.4.2 Left O~우터 조인 
테이블 
‘A’와 
‘B ’가 있고 A를 왼쪽 테이블，B를 오른쪽 테이블이라고 가정합나다. 
A 태이블과 B 테이블을 조인할 때 A 태이블의 결과 집합은 모두 보여 주면서 B 
테이블은 조인에 성공한 데이터만 보여 주고 조인이 실패한 B 태이블의 데이터는 
보여주지 않습니다， 왼쪽 테이블인 A 태이블의 결과 집합은 조인의 성공 여부에 
영향을 받지 않지만， 오른쪽 테이블인 B 태이블은 조인의 성공 여부에 따라 보일 
수도 있고 안 보일 수도 있습니다. 이러한 방식을 Left 아우터 조인이라고 합니다 
다음예제로 정리하면，A 테이블을 기준으로B 테이블과조인하고A 테이블의 모 
든데이터는출력이 보장되며 B 태이블의 데이터는조인에 성공한것은보여 주고 
조인에 성공하지 않은 것은 보여 주지 않습니다 
SELECT * 
FROM A 
LEFT OUTER JOIN B 
ON A.NO = B.NO; 
100 -

---
<!-- PDF p.100 -->

3.4.3 Right 아우터 조인 
테이블 
‘A’와 ‘B’가 있고 A 테이블은 왼쪽 테이블.B 테이블은 오른쪽 테이블이라 
고가정합니다. 테이블A와B를조인할때 B 테이블의 결과집합은모두보여 주 
면서 조인이 성공한 A 테이블의 데이터만 보여 주고 조인이 실패한 A 테이블의 
데이터는 보여 주지 않습나다. 오른쪽 테이블인 B 테이블의 결과 집합은 조인의 
성공 여부에 영향을 받지 않지만， 왼쪽 테이블인 A 테이블은 조인의 성공 여부에 
따라 해당 데이터가 보일 수도 있고 안 보일 수도 있습니다. 이러한 방식을 Right 
아우터 조인이라고합니다. 
다음 예제로 정리하면.B 테이블을 기준으로 A 테이블과 조인하고 B 테이블의 모 
든 데이터는 출력이 보장되며 A 테이블의 데이터는 조인에 성공한 것은 보여 주 
고 조인에 성공하지 않은 것은 보여 주지 않습니다. 
SELECT * 
FROM A 
RIGHT OUTER JOIN B 
ON B.NO = A.NO; 
3.4.4 아우터조인튜닝 
아우터 조인 튜닝은 아우터 조인으로 구현된 SQL문의 성능을 극대화하는 일련의 
모든 활동을 의미합니다. 대표적으로 다음 두 가지 경우에 아우터 조인 튜닝을 할 
수있습니다 
야우터 조인을 이용하여 테이를 스캔 최소화 
테이블 ‘A’, ‘B’, ‘C’가 있다고 가정합니다. 
‘A=B+C’의 관계가 성립할 경우 테 
이블 
B와 테이블 
C는 테이블 A에 대하여 베타적 관계에 있다고 할 수 있습니다. 
테이블 
A를 기준으로 테이블 
B와 
C를 각각 조인할 때 UNION ALL문을 사용하여 
3 조인 튜닝 -101

---
<!-- PDF p.101 -->

두 개의 SELECT문의 합집합을 구하게 됩니다. 이러한 경우 UNION 
ALL문을 아 
우터 조인으로 변환하여 성능을 개선할 수 있습니다(가장큰 태이블인 A를 단 한 번만 
스캔하는 것이 핵심입니다) . 
아우터 조인을 스칼랴 
서브쿼2. 1로 
아우터 조인으로 구현된 SQL문은 스칼라 서브쿼리Scalar S뼈u e r }'로 변환할 수 있 
습니다. 오라클에서는 스칼라 서브쿼리로 한 번 이상 호출된 Input/Output 값 
을 멀티 버퍼에 저장해 둔 후 동일한 Input으로 호출되면 기존에 가지고 있던 
Output 값을 바로 리턴하는 스칼라 서브쿼리 캐싱 기능이 있습니다. 이러한 오 
라클의 기능을잘활용하여 아우터 조인을스칼라서브쿼리로 변환하면 성능을극 
대화할수있습니다. 
이 2가지 경우를 실습을 통해 자세히 알아보겠습니다 
실습 3-5 O ~우터 조인므로 테이블 스캔율 최소화하여 성능 개선하기 
A 실습준비 
A-1 테이블생성 
TB~OB_ORDER 테이블을 
생성합니다 
CREATE TABLE TB JOB ORDER 
JOB_NO VARCHAR2(10) , 
VISIT_PRCDT VARCHAR2(8) , 
WORKER_ID VARCHAR2(10), 
JOB_STATUS_CD VARCHAR2(2) , 
JOB_GUBUN VARCHAR2(1), 
REQ_NO VARCHAR2(10), 
INST DTM DATE 
INST_ID VARCHAR2(50) 
102-

---
<!-- PDF p.102 -->

TB_OPEN_REQ 태이블을 
생성합니다. 
CREATE TABLE TB_OPEN_REQ 
OPEN_RE(LNO VARCHAR2 (10), 
OPEN_RE(LDT VARCHAR2 (8) , 
OPEN_HOPE_DT VARCHAR2 (8) , 
CUST_NO VARCHAR2(10), 
INST DTM DATE 
INST_ID VARCHAR2(50) 
TB _D ISABLE_REQ 테이블을 
생성합니다. 
CREATE TABLE TB_DISABLE_REQ 
DISABLE_RE(LNO VARCHAR2(10) , 
DI SABLE_RE(LDT VARCHAR2(8), 
DISABLE DTM DATE 
CUST_NO VARCHAR2(10 ), 
INST DTM DATE 
INST_ID VARCHAR2(50 ) 
A-2 데이터입력 
TB_OPEN_REQ 테이블에 100만건의 데이터를입력합니다 
INSERT INTO TB_OPEN_REQ 
SELECT 
LPAD(TO_CHAR(ROWNUMl, 10, '0 ’ ), 
RANDOM DT 
TO_CHAR(TO_DATE (RANDOM_DT, ’YYYYMMDD') + 
DBMS_RANDOM. VALUE( 1, 30) , 
'YYYYMMDD ’), 
LPAD(TO_CHAR(TRUNCWBMS_RANDOM.VALUE (l , 10000))) , 10 , '0') , 
SYSDATE 
'DBMSEXPERT ‘ 
FROM 
3 조인튜닝 - 103

---
<!-- PDF p.103 -->

SELECT 
TO_CHAR(SYSDATE - DBMS_RANDOM.VALUE(l , 3650), 
’ YYYYMMDD' ) RANDOM_DT 
FROM DUAL 
) A, DUAL CONNECT BY LEVEL <= 1000000; 
COMMIT; 
TB_DISABLE_REQ 테이블에 100만 건의 데이터를 입력합니다 
INSERT INTO TB_DISABLE_REQ 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 10, ’0’), 
RANDOM_DT, 
TO_DATE (RANDOM• DT: :TO• CHAR(SYSDATE-864000/24/60/60 , 'HH24MISS' ), 
'YYYYMMDDHH24MISS ’ ), 
LPAD(TO_CHAR(TRUNCWBMS_RANDOM .VALUE (l , 10000 ))) , 10, '0 ’ ), 
SYSDATE 
'DBMSEXPERT' 
FROM 
SELECT 
TO_CHAR(SYSDATE - DBMS_RANDOM.VALUE(l ι 
3650)ι ‘ YYYYMMDD ' ) RANDOM_DT 
FROM DUAL 
) A, DUAL CONNECT BY LEVEL <= 1000000; 
COMMIT; 
TB-.JOB_ORDER 테이블에 200만 건의 데이터를 입력합니다. 
INSERT 
1싹 APPEND *1 INTO TB_JOB_ORDER - APPEND 힌트 사용 
SELECT 
LPAD(TO_CHAR(ROWNUM ), 10, '0 ’ ), 
TO_CHAR(SYSDATE - DBMS_RANDOM .VALUE(1 , 3650 ), 'YYYYMMDD' ), 
LPAD(TO_CHAR(TRUNCWBMS_RANDOM.VALUE (l , 10000))) , 10, '0 ‘ ), 
LPAD(TO_CHAR(TRUNC(MOD(DBMS_RANDOM.VALUE (l , 
10000 ) ι 10))) , 2, ’0' ), 
JOB GUBUN 
REQ_NO , 
SYSDATE 
'DBMSEXPERT' 
104 -

---
<!-- PDF p.104 -->

FROM DUAL 
(SELECT ’1 ’ JOB_GUBUN , OPEN_RE~NO RE~NO 
FROM TB_OPEN_REQ 
UNION ALL 
SELECT 
‘2' JOB_GUBUN , DISABLCRE~NO REQ_NO 
FROM TB_DISABLE_REQ 
) A; 
COMMIT; 
A-3 기본키생성 
데이터를 입력한후 기본커를 생성합니다 
ALTER TABLE TB JOB ORDER 
ADD CONSTRAINT TB JOB ORDER PK 
PRIMARY KEY (JOB_NO); 
ALTER TABLE TB_OPEN_REQ 
ADD CONSTRAINT TB_OPEN_RE~PK 
PRIMARY KEY 
(O PEN_RE~NO ) ; 
ALTER TABLE TB_DISABLE_REQ 
ADD CONSTRAINT TB_DISABLE _RE~PK 
PRIMARY KEY 
( DISABLE _RE ~NO ); 
A-4 통계정보생성 
테이블과 인텍스에 대한통계정보를생성합니다. 
ANALYZE TABLE TB JOB ORDER COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB_OPEN_REQ COMPUTE STATISTICS FOR TABLE 
FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB_DISABLE_REQ COMPUTE STATISTICS FOR TABLE 
FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
3 조인 튜닝 - 105

---
<!-- PDF p.105 -->

B. 튜닝전상황 
B-1 튜닝전SQL문 
SELECT A.JOB_NO , A.WORKER_ID, A.JOB_STATUS_CD , A.REQ_NO 
2 
FROM TUOB_ORDER A, TB_OPEN_REQ B 
3 
WHERE A.VISIT PRE DT 
’ 20151102 ’ 
4 
AND A.JOB_GUBUN 
‘ l' 
5 
AND A.REQ_NO = B.OPEN_REQ_NO 
6 
UNION ALL 
7 
SELECT A.JOB_NO , A.WORKER_ID, A.JOB_STATUS_CD , A.REQ_NO 
8 
FROM TB_JOB_ORDER A, TB_DISABLE_REQ B 
9 
WHERE A.VISIT PRE DT = ’20151102 ’ 
10 AND A.JOB GUBUN 
‘ 2’ 
11 
AND A.RE(LNO = B.DISABLE_REQ_NO; 
SQL 분석 
2번째 줄 TB-.JOB_ORDER 테이블과 TB_OPEN_REQ 태이블을 조인합니다 
8번째 줄 TB-.JOB_ORDER 테이블과 TB_DISABLE_REQ 태이블을 
조인합니다. 
6번째줄각각의 SEELCT문을UNION ALL하여 결과집합을도출합니다. 
SQL 문제점 
이 SQL에서는 가장 큰 용량의 테이블인 TB-.JOB_ORDER 태이블을 두 번이나 
스캔하고 있습니다. 해당 테이블을 단 한 번만 스캔하여 결과 집합을 도출한다면 
훨씬 더 성능이 좋은 
SQL문이 될 수 있습니다. 
수행시간 
0 .9초 
8-2 실행계획 
10 
O 
106 
Opell.ition 
SELECT STATEMENT 
UNION-ALL 
Name 
Cost 
11333

---
<!-- PDF p.106 -->

c。앙 
5666 
5466 
5666 
5466 
Name 
Operation 
10 
TB JOB ORDER 
TB_OPEN_RECLPK 
NESTED LOOPS 
TABLE ACCESS FULL 
INDEX UNIQUE SCAN 
2 
1
」
A‘T 
F
늬
}
 
TB JOB ORDER 
TB_DISABLE_RECLPK 
NESTED LOOPS 
TABLE ACCESS FULL 
INDEX UNIQUE SCAN 
6 
7 
수행순서(ID 기준) 
3 • 4 • 2 • 6 • 7 • 5 • 1 • O 
실행계획설명 
TB_JOB_ORDER 테이블을 풀 스캔(TABLE ACCESS FUL L)합니다 
TB_OPEN_REQ_PK 인돼스를 인댁스 유일 스캔(INDEX UNIQUE SCAN)합니다 
3번과 
4번을 중접 루프 조인 (NESTED LOOPS)합니다 
TB JOB ORDER 테이블을 풀 스캔 (TABLE ACCESS FUL L)합니다 
TB_DISABLE_REQ_PK 인댁스를 인돼스 유일 스캔(INDEX UNIQUE SCAN)합니다 
6번과 
7 번을 중접 루므 조인(N ESTED LOOPS)합니다 
2 번과 
3 번을 UNION ALL합니다 
SELECT문을 종료합니다 
설명 
10 
4 
〔
·
ι
FO 
「
/
「
。
,1 
nU 
3 
C. 튜닝 
C-1 튜닝후SQL문 
’l 
?‘ 
「
3
A
아
 
「
3
ζ
ν
 
7’ 
SELECT 
/*+ LEADING(A) USUL(B) USUL(C) */ 
A.JOB_NO, A‘ WORKER_ID, A.JOUTATUS_CD , A.REQ_NO 
FROM TB JOB ORDER A 
TB_OPEN_REQ B, 
TB_DISABLE_REQ C 
WHERE A.VISIT PRE DT = ‘20151102' 
3 조인튜님 - 107

---
<!-- PDF p.107 -->

8 
AND DECODE(A.JOB_GUBUN, ’1 ’, A.RE(LNO) = B.OPEN_REQ_NO(+) 
9 
AND DECODE(A.JOB_GUBUN, ’2’, A.REQ_NO) = C.DISABLE_REQ_NO(+); 
SQL 분석 
2번째 줄 LEADING 힌트를 이용하여 TB..JOB_ORDER 테이블을 Outer 테이블 
로 지정하고， USE_NL 힌트를 이용하여 TB_OPEN_REQ 테이블을 Inner 테이 
블로 지정합니다 TB_ORD_ORDER 테이블과 TB_OPEN_REQ 태이블의 중 
첩 루프 조인 결과가 나오변 해당 결과를 Outer 테이블로 하고， TB_DISABLE_ 
REQ 테이블을Inner 태이블로하여 중첩 루프조인합니다. 
8번째 줄 TB_ORD_ORDER 테이블의 ]OB_GUBUN의 값이 
‘ 1 ’ 인 경우 TB 
OPEN_REQ 테이블과 Left 아우터 조인합니다 
9번째 줄 TB_ORD_ORDER 테이블의 ]OB_GUBUN의 값이 
‘2’인 경우 TB 
DISABLE_REQ 테이블과 Left 아우터 조인합니다 
수행시간 
0 .5초(기존 
0.9초) 
C-2 실행계획 
ID 
Operation 
G 
SELECT STATEMENT 
NESTED LOOPS OUTER 
2 
NESTED LOOPS OUTER 
3 
TABLE ACCESS FULL 
4 
INDEX UNIQUE SCAN 
5 
INDEX UNIQUE SCAN 
수행순서(ID 기준) 
3 • 4 • 2 • 5 •
1 • O 
108 -
Name 
Cost 
45492 
45492 
25479 
TB J OB ORDER 
5466 
TB_OPEN_REQ_PK 
TB_DISABLE_REQ_PK

---
<!-- PDF p.108 -->

실행계획설명 
10 
설명 
3 
TB_JOB_ORDER 테이블을 풀 스캔(TABLE ACCESS FUL L)합니다 
4 
TB_OPEN_REQ_PK 인댁스를 인댁스 유일 스캔(INDEX UNIQUE SCAN)합니다 
2 
3 번과 4번을 중접 루프 아우터 조인 (NESTED LOOPS OUTER)합니 다 
5 
TB_DISABLE_REQ_PK 인댁스를 인댁스 유일 스캔( INDE X UNIQUE SCAN)합니다 
2 번과 5번을 중접 루프 아우터 조인(N ESTED LOOPS OUTER)합니다 
o 
S ELECT절의 연산을 수행합니다 
실습 
3-6ot우터 조인을 스칼라 서브쿼리 방식으로 변환하여 성능 
극대호}하71 
A. 설슴준비 
A-1 테이블생성 
TB TRD DAY 테이블을 생성합니다 
CREATE TABLE TB TRD DAY 
TRD_DT VARCHAR2(8) , -거래일자 
INSU_CD 
VARCHAR2(4), 
인수코드 
INSU_DETAIL_CD VARCHAR2(6) , -인수상세코드 
TRD_CNT 
NUMBER , 
거래건수 
CNCL_CNT NUMBER, 
취소건수 
EXPORTER NO 
VARCHAR2 ( 10 )
-수출자번호 
TB_EXPORTER 테이블을 
생성합니다. 
CREATE TABLE TB EXPORTER 
EXPORTER_NO 
VARCHAR2 (10), -수출자번호 
EXPORTER NM VARCHAR2 (50 ) -수출자명 
3 조인튜님 - 109

---
<!-- PDF p.109 -->

A-2 데이터입력 
TB EXPORTER 테이블에 10만 건의 데이터를 입력합니다. 
INSERT INTO TB EXPORTER 
SELECT 
LPAD(TO_CHAR(ROWNUM) , 10 , '0' ), 
DBMS_RANDOM .STRING( 'U ’, 50 ) 
FROM DUAL CONNECT BY LEVEL <= 100000; 
COMMIT; 
TB_TRD_DAY 테이블을 NOLOGGING 모드로 설정합니다 
ALTER TABLE TB TRD DAY NOLOGGING; 
TB_TRD_DAY 테이블에 100만 건의 데이터를 입력하였습니다 
INSERT /*+ APPEND */ INTO TB_TRD_DAY A -APPEND 힌트 사용 
SELECT 
TO_CHAR(SYSDATE - TRUNC(DBMS_RANDOMVALUE(0, 3650)) , ‘YYYYMMDD ’ ), 
LPAD(TO_CHAR(TRUNCWBMS_RANDOM.VALUE(1 , 9999))) , 4, ’0’ ), 
LPAD(TO_CHAR(ROWNUM) , 6, 
‘0' ), 
TRUNCWBMS_RANDOM.VALUE(l , 999 )) , 
TRUNC(DBMS_RANDOM.VALUE (l , 999 )) , 
B. EXPORTER NO 
FROM TB_EXPORTER B , (SELECT LEVEL LV FROM DUAL CONNECT BY LEVEL <= 10); 
COMMIT; 
A-3 기본키생성 
데이터를 입력한 후 기본키를 생성합니다 
ALTER TABLE TB TRD DAY 
ADD CONSTRAINT TB TRD DAY PK 
PRIMARY KEY (TRD_DT , INSU_CD , INSU_DETAIL_CD); 
ALTER TABLE TB EXPORTER 
110 -

---
<!-- PDF p.110 -->

ADD CONSTRAINT TB EXPORTER PK 
PRIMARY KEY (EXPORTER_NO ); 
A-4 통계정보생성 
테이블과 인텍스에 대한통계정보를생성합니다. 
ANALYZE TABLE TB EXPORTER COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
ANALYZE TABLE TB TRD DAY COMPUTE STATISTICS 
FOR TABLE FOR ALL INDEXES FOR ALL INDEXED COLUMNS SIZE 254; 
격B. 
8-1 튜닝전SQL문 
SELECT 
2 
1*+ LEADING(A) USE_NL(B) INDEX(A TB_TRD_DAY_PK) */ 
3 
A. EXPORTER NO 
4 
B. EXPORTER NM 
5 
SUM(A.TRD_CNT - A.CNCL_CNT) TOT_CNT 
6 
FROM TB_TRD_DAY A LEFT OUTER JOIN 
7 
TB_EXPORTER B 
8 
ON ( A. EXPORTER_NO = B. EXPORTER_NO) 
9 
AND A.TRD DT BETWEEN ’20100101 ’ AND ’20151231 ’ 
10 GROUP BY A.EXPORTER NO. B.EXPORTER NM 
11 
ORDER BY A. EXPORTER_NO; 
SQL 분석 
2번째 줄 LEADING 힌트를 이용하여 TB_TRD_DAY 테이블 Outer 테이블로 
지정합니다. USE_NL 힌트를 이용하여 TB_EXPORTER 테이블을 Inner 테이블 
로 지정하고 중첩 루프 조인 처리가 되도록 합니다. INDEX 힌트를 이용하여 TB 
TRD_DAY_PK 인텍스를 시용하도록 
합니다. 
6번째 줄 TB TRD DAY 태이블을 기준으로 Left 아우터 조인 처 리를 
합니다. 
3 조인튜닝- 111

---
<!-- PDF p.111 -->

7번째 줄 TB_EXPERTER 테이블은 Outer 테이블이 되었습니다， 
SQL의문제점 
Outer 테이블인 TB_EXPORTER 테이블 내에 EXPORER NM 컬럼만 
SELECT절 
에 있습니다 이러한 경우 TB EXPORTER 테이블을 반드시 아우터 조인할 필요 
없으므로스칼라서브쿼리로변환한다면성능향상을꾀할수있습니다. 
수행시간 
9.2초 
8-2 실행계획 
10 
Operation 
0 
SELECT STATEMENT 
SORT GROUP BY 
2 
NESTED LOOPS OUTER 
3 
TABLE ACCESS BY INDEX ROWID 
4 
INDEX FULL SCAN 
5 
TABLE ACCESS BY INDEX ROWID 
6 
INDEX UNIQUE SCAN 
수행순서(l D 기준) 
4 •
3 •
6 •
5 •
2• 
1 • O 
실행계획설명 
Narne 
Cost 
2005426 
2005426 
2005362 
TB TRD DAY 
1004715 
TB TRD DAY PK 
4348 
TB EXPORTER 
TB EXPORTER PK 
0 
4 
TB_ TRD_DAY _PK 인텍스를 인텍스 풀 스캔(TABLE ACCESS FUL L)합니다 
3 
4 번에서 나온 ROWID를 이용하여 TB_TRD_DAY 테이블을 테이블 랜덤 액세스(TABLE 
ACCESS BY INDEX ROWID)합니다 
6 
TB EXPROTER PK 인텍스를 인텍스 유일 스캔( INDEX UNIQUE SCAN)합니다 
5 
6 번에서 ROWID를 이용하여 TB_EXPROTER 테이블을 테이블 랜덤 액셰스(TABLE 
ACCESS BY INDEX ROWID)합니다 
112

---
<!-- PDF p.112 -->

10 
설명 
2 
3 번과 5번을 중접 루프 아우터 조인(N E STED LDDPS OUTER)합니다 
SORT와 GROUP BY 연산을 수행합니다 
o 
SELECT절의 연산을 수행합니다 
C 튜닝 
C-1 튜닝후SQL문 
SELECT 
2 
X.EXPORTER NO 
3 
(SELECT B.EXPORTER_NN 
4 
FRON TB_EXPORTER B 
5 
WHERE 
6 
B.EXPORTER_NO = X.EXPORTER_NOL TOLCNT 
7 
FROM 
8 
(SELECT 
9 
A.EXPORTER_NO , 
10 
SUM(A.TRD_CNT-A.CNCUNT) TOUNT 
11 
FRON TB_TRD_DAY A 
12 WHERE 
13 
A.TRD_DT BETWEEN ‘20100101' AND ’20151231' 
14 
GROUP BY A.EXPORTER_NO 
15 
ORDER BY A.EXPORTER_NO 
16 ) X ; 
SQL 분석 
8-16번째 줄 TB_TRD_DAY 테이블을 인라인 뷰를 이용하여 원하는 데이터를 
가져왔습니다. 
3-6번째 줄 스칼라 서브쿼리를 이용하여 TB_EXPORTER 테이블을 스캔하여 
EXPORTER_NM을가져오게 하였습니다. 스칼라서브쿼리의 캐싱 효과로 인해 
성능이향상됩니다. 
3 조인 튜닝 - 113

---
<!-- PDF p.113 -->

수행시간 
1. 7초(기존 
9.2초) 
C-4 실행계획 
10 
Operatíon 
Name 
Cost 
0 
SELECT STATEMENT 
1773 
TABLE ACCESS BY INDEX ROWID 
TB EXPORTER 
2 
2 
INDEX UNIQUE SCAN 
TB EXPORTER PK 
3 
VIEW 
1773 
4 
SORT GROUP BY 
1773 
5 
TABLE ACCESS FULL 
TB TRD DAY 
1736 
수행 순서(1 0 기준) 
5 • 4 • 3 • 2 •
1 • O 
실행계획설명 
싫영 
여 
5 
TB TRO OAY 테이블을 테이블 풀 스캔(TABLE ACCESS 
FUL L)합니다 테이블 울 스캔이 
더 유리하다는 옵티마이저의 판단입니다 
4 
SORT와 GROUP BY 연산을 수행합니다 
3 
해당 결과를 인라인 뷰(VI E W)로 생성합니다 
2 
TB_EXPORTER_PK 인텍스를 인텍스 유일 스캔(INDEX UNIQUE 
SCAN)합니다 
EXPORTER_NM 걸럼의 값을 가져오기 위해 테이블 랜덤 액세스(TAB LE ACCESS BY 
INDEX ROWID)를 수행합니다 
o 
SELECT절의 연산을수행합니다 
1 4