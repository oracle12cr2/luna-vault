# 2026-03-23 (월)

## 북케이스 프로젝트
- OCR 텍스트 레이어 안 되는 문제: Helvetica(영문전용) → korea(CJK) 폰트 교체 필요
- 남궁건 결정: 북케이스 삭제, 유나한테 맡기기로 (옵시디언 활용)
- webserver01/02에서 /books, /viewer, BookShelf.js, BookGuard.js 전부 삭제
- 로컬 books/ 폴더(PDF, OCR 스크립트)도 삭제

## 로또 위젯 수정
- 문제: buy_log.txt 파싱에서 `=` 50개 split → 실제 로그는 `[타임스탬프] ===...===` 형태라 매칭 안 됨
- 수정: `🎰 동행복권 로또6/45 자동 구매` 문자열 기준으로 블록 분리
- 결과: 1217회차(오늘) 정상 반영, Total 8건(성공4/실패4)

## 주식/데이트레이딩
- 오늘 관망하기로 했으나 crontab이 살아있어서 14:50 자동 청산 실행됨
- 손실: ACE200 -43,645(-1.6%), TIGER반도체 -415,590(-5.5%), 합계 -459,235원
- **승인 모드 추가**: --mode buy → 분석만 하고 알림, buy-exec로 승인 후 실행
- **손절 -3% 추가**: --mode stoploss, 장중 10분마다 체크, -3% 이하 즉시 자동 매도
- crontab: */10 9-14 * * 1-5 stoploss 추가
- 모의투자 잔고: 전량 매도 상태 (예수금만 남음)

## 인텔 맥북 정리 (192.168.50.158)
- 호스트명: oracle10g.local, CPU: i9-9880H, 32GB RAM, 500GB SSD, macOS 15.7.4
- 유저: oracle
- **삭제한 앱/데이터** (~55GB 확보):
  - Steam (25GB), NoxAppPlayer (11GB), Anaconda (5.1GB), Google Drive (7GB+777MB)
  - Discord (441MB+1.5GB), Notion (2.2GB+274MB), GitHub Desktop (411MB)
  - DBeaver Ultimate (391MB), Plex (315MB), qbittorrent (81MB), KakaoTalk (133MB)
  - 캐시 정리 5.3GB, VS Code 구버전 확장+캐시 1.6GB
  - instantclient (729MB), ~/opt/oracle (242MB), node_modules (428MB)
  - 각종 찌꺼기 (oradiag, Google_Drive, pg_python, db2jcc.jar 등)
- **보류**: MS Office, VS Code, XQuartz, HEIC converter
- Claude Code 업데이트: 2.1.76 → 2.1.81
- SSH 키 생성 (ed25519) → 50.56 oracle/kto2005 접속 등록 완료

## SSH/계정 정리
- 50.56 kto2005 계정: ed25519 키 생성, 전체 VM SSH+sudo 등록
- Oracle DB 서버 6대(.21~.23, .31/.32, .41): kto2005 계정 생성 + SSH키 + sudo NOPASSWD
- grid 계정 6대: sudo NOPASSWD 등록 (/etc/sudoers.d/grid)
- postgres01(.16): kto2005 SSH키 추가 등록
- 전체 16대 VM kto2005 SSH+sudo 확인 완료

## PostgreSQL 업그레이드
- postgres01(.16): 16.13 → 17.9 업그레이드 완료
- pg_upgrade --old-bindir=/usr/bin --new-bindir=/usr/pgsql-17/bin
- PGDG 리포 추가, postgresql-17 서비스로 전환
- .bash_profile PGHOST=localhost 때문에 peer 인증 안 먹히는 이슈 (PGHOST= 로 우회)
- listen_addresses='*', pg_hba.conf 복사, vacuumdb 완료
- 남궁건 이유: 서적이 PG 17 기준

## Oracle RAC 성능 튜닝 가이드
- Part20_Oracle_RAC_성능튜닝.md 작성 (1,535줄, 10개 섹션)
- 서브에이전트로 작성, GitHub push 완료 (oracle12cr2/oracle-sql-tuning-compass)

## Luna Dashboard
- 메모리 뷰어에 🌙루나/🌸유나 탭 전환 추가
- /api/memory-yuna 엔드포인트: SSH로 유나 맥북에서 실시간 데이터 가져옴
- MEMORY.md 유나 맥북으로 복사 완료

## OpenClaw 보이스 모드
- 남궁건이 구형 맥북에서 보이스 모드 + 리모컨 설정 시도 중
- pyobjc-framework-Quartz 설치 완료 (Python 3.8 → 3.12 필요했음)

## Kafka → PostgreSQL Sink 연동 (진행 중)
- Kafka 브로커 복구: meta.properties 손상 → cluster.id=HOqKn7hRSba5Xbx8-jzUhQ, node.id=1 재생성
- postgres-sink-connector 생성 완료 (RUNNING)
  - topics: PROD.SH.*, PROD.PM.ONLINE_MEDIA (11개 토픽)
  - insert.mode=insert, pk.mode=none
  - transforms: unwrap(Debezium) + rename(PROD.SH.SALES → SH_SALES)
  - consumer.override.auto.offset.reset=earliest
  - connection: kafka_sink/kafka2026@192.168.50.16:5432/postgres
- PostgreSQL: kafka_sink 유저, sh/pm/hr 스키마 생성 완료
- ⚠️ **미해결**: 토픽에 데이터 0건 — Debezium Source(oracle-source-pm-sh)가 CDC 변경 캡처 못 하는 중
  - Oracle UPDATE 넣었는데 토픽에 안 들어옴
  - LogMiner 세션 문제 가능성 → Connect 로그 상세 디버깅 필요
  - Source 설정: debezium@192.168.50.35:1521/PROD, schema=PM,SH, snapshot.mode=initial

## 내일 (3/24) TODO
- [ ] Oracle Data Guard(.41) 패치
- [ ] Debezium LogMiner CDC 디버깅 (토픽 데이터 0건)
- [ ] 데이트레이딩 승인 모드 첫 테스트 (09:30 알림)

## Kafka Oracle-to-Oracle 파이프라인 (보류)
- OGG RAC(.35) → Kafka → 3노드 RAC(.27) 새로 구성 예정
- 기존 Source(oracle-source-pm-sh) 토픽 비어있는 문제 해결 후 진행
- Source 재생성 (snapshot.mode=initial) + Sink 재설정 필요
- Debezium LogMiner CDC 디버깅 먼저 해결해야 함

## 구매 예정
- [ ] 블루투스 셀카 리모컨 — 인텔 맥북 보이스 모드 리모컨용 (다이소/쿠팡, 2~3천원)

## 인텔 맥북 정보 (TOOLS.md 참고용)
- 호스트명: oracle10g.local (macbook-intel)
- IP: 192.168.50.158
- SSH: oracle@192.168.50.158 (ed25519 키 인증)
- OS: macOS 15.7.4 Sequoia (Intel x86_64)
- CPU: i9-9880H / RAM: 32GB / SSD: 500GB
